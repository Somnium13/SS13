// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Structure_Closet_SecureCloset_Personal : Obj_Structure_Closet_SecureCloset {

		public string registered_name = null;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.req_access = new ByTable(new object [] { 21 });
		}

		// Function from file: personal.dm
		public Obj_Structure_Closet_SecureCloset_Personal ( dynamic loc = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			Task13.Schedule( 2, (Task13.Closure)(() => {
				
				if ( Rand13.PercentChance( 50 ) ) {
					new Obj_Item_Weapon_Storage_Backpack( this );
				} else {
					new Obj_Item_Weapon_Storage_Backpack_SatchelNorm( this );
				}
				new Obj_Item_Device_Radio_Headset(  );
				return;
			}));
			return;
		}

		// Function from file: personal.dm
		public override dynamic togglelock( dynamic user = null, string given_name = null ) {
			given_name = given_name ?? "";

			dynamic O = null;

			
			if ( this.allowed( user ) || !Lang13.Bool( this.registered_name ) || this.registered_name == given_name ) {
				this.locked = !this.locked;

				foreach (dynamic _a in Lang13.Enumerate( Map13.FetchViewers( 3, user ) )) {
					O = _a;
					

					if ( Lang13.Bool( O.client ) && !Lang13.Bool( O.blinded ) ) {
						GlobalFuncs.to_chat( O, "<span class='notice'>The locker has been " + ( this.locked ? null : "un" ) + "locked by " + user + ".</span>" );
					}
				}

				if ( this.locked ) {
					this.icon_state = this.icon_locked;
				} else {
					this.icon_state = this.icon_closed;
				}

				if ( !Lang13.Bool( this.registered_name ) && Lang13.Bool( given_name ) ) {
					this.registered_name = given_name;
					this.desc = "Owned by " + given_name + ".";
				}
			} else {
				GlobalFuncs.to_chat( user, "<span class='notice'>Access Denied.</span>" );
			}
			return null;
		}

		// Function from file: personal.dm
		public override dynamic attackby( dynamic a = null, dynamic b = null, dynamic c = null ) {
			dynamic I = null;

			
			if ( a is Obj_Item_Weapon_Card_Id ) {
				
				if ( this.broken ) {
					GlobalFuncs.to_chat( b, "<span flags='rose'>It appears to be broken.</span>" );
					return null;
				}
				I = a;

				if ( !Lang13.Bool( I ) || !Lang13.Bool( I.registered_name ) ) {
					return null;
				}
				this.togglelock( b, I.registered_name );
			} else {
				base.attackby( (object)(a), (object)(b), (object)(c) );
			}
			return null;
		}

	}

}