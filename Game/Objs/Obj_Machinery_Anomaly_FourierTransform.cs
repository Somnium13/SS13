// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Machinery_Anomaly_FourierTransform : Obj_Machinery_Anomaly {

		// Function from file: analysis_fourier_transform.dm
		public Obj_Machinery_Anomaly_FourierTransform ( dynamic loc = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			this.component_parts = new ByTable(new object [] { 
				new Obj_Item_Weapon_Circuitboard_Anom(), 
				new Obj_Item_Weapon_StockParts_ScanningModule(), 
				new Obj_Item_Weapon_StockParts_ScanningModule(), 
				new Obj_Item_Weapon_StockParts_ScanningModule()
			 });
			this.RefreshParts();
			return;
		}

		// Function from file: analysis_fourier_transform.dm
		public override string ScanResults(  ) {
			string results = null;
			dynamic scanned_sample = null;
			string carrier = null;
			int num_reagents = 0;
			Reagent A = null;
			Reagent R = null;
			int specifity = 0;
			double distance = 0;

			results = "The scan was inconclusive. Check sample integrity and carrier consistency.";
			num_reagents = 0;

			foreach (dynamic _a in Lang13.Enumerate( this.held_container.reagents.reagent_list, typeof(Reagent) )) {
				A = _a;
				
				R = A;

				if ( R is Reagent_AnalysisSample ) {
					scanned_sample = R.data;
				} else {
					carrier = R.id;
				}
				num_reagents++;
			}

			if ( num_reagents == 2 && Lang13.Bool( scanned_sample ) && Lang13.Bool( carrier ) ) {
				specifity = this.GetResultSpecifity( scanned_sample, carrier );
				distance = scanned_sample.artifact_distance;

				if ( distance > 0 ) {
					distance += ( Rand13.Float() * 2 - 1 ) * distance * 0.05;
					results = "Fourier transform analysis on anomalous energy absorption through carrier (" + carrier + ") indicates source located inside emission radius (" + specifity * 95 + "% accuracy): " + distance + ".";
				} else {
					results = "Energy dispersion detected throughout sample consistent with background readings.<br>";
				}

				if ( carrier == scanned_sample.source_mineral ) {
					results += "Warning, analysis may be contaminated by high quantities of molecular carrier present throughout sample.";
				}
			}
			return results;
		}

	}

}