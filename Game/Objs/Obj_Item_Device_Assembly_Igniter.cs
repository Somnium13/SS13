// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Device_Assembly_Igniter : Obj_Item_Device_Assembly {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.starting_materials = new ByTable().Set( "$iron", 500 ).Set( "$glass", 50 );
			this.wires = 1;
			this.icon_state = "igniter";
		}

		public Obj_Item_Device_Assembly_Igniter ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: igniter.dm
		public override dynamic attack_self( dynamic user = null, dynamic flag = null, bool? emp = null ) {
			this.activate();
			this.add_fingerprint( user );
			return null;
		}

		// Function from file: igniter.dm
		public override bool activate(  ) {
			Ent_Static grenade = null;
			dynamic location = null;
			Ent_Static tank = null;
			Effect_Effect_System_SparkSpread s = null;

			
			if ( !base.activate() ) {
				return false;
			}

			if ( this.holder != null && this.holder.loc is Obj_Item_Weapon_Grenade_ChemGrenade ) {
				grenade = this.holder.loc;
				((dynamic)grenade).prime();
			} else {
				location = GlobalFuncs.get_turf( this.loc );

				if ( Lang13.Bool( location ) ) {
					((Tile)location).hotspot_expose( 1000, 1000, null, this.loc is Tile );
				}

				if ( this.loc is Obj_Item_Device_AssemblyHolder ) {
					
					if ( this.loc.loc is Obj_Structure_ReagentDispensers_Fueltank ) {
						tank = this.loc.loc;

						if ( tank != null && Lang13.Bool( ((dynamic)tank).modded ) ) {
							((Obj_Structure_ReagentDispensers_Fueltank)tank).explode();
						}
					}
				}
				s = new Effect_Effect_System_SparkSpread();
				s.set_up( 3, 1, this );
				s.start();
				return true;
			}
			return false;
		}

	}

}