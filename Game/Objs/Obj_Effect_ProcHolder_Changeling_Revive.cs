// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Effect_ProcHolder_Changeling_Revive : Obj_Effect_ProcHolder_Changeling {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.req_stat = 2;
		}

		public Obj_Effect_ProcHolder_Changeling_Revive ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: revive.dm
		public override bool can_be_used_by( Mob user = null ) {
			bool _default = false;

			
			if ( user.stat != 2 && !( ( user.status_flags & 8192 ) != 0 ) ) {
				user.mind.changeling.purchasedpowers.Remove( this );
				return false;
			}
			_default = base.can_be_used_by( user );
			return _default;
		}

		// Function from file: revive.dm
		public override dynamic sting_action( Mob user = null, Ent_Static target = null ) {
			Mob H = null;

			
			if ( user.stat == 2 ) {
				GlobalVars.dead_mob_list.Remove( user );
				GlobalVars.living_mob_list.Add( user );
			}
			user.status_flags &= 57343;
			user.stat = 1;
			((dynamic)user).tod = null;
			((dynamic)user).toxloss = 0;
			((dynamic)user).oxyloss = 0;
			((dynamic)user).cloneloss = 0;
			user.paralysis = 0;
			user.stunned = 0;
			user.weakened = 0;
			user.radiation = 0;
			user.sleeping = 0;
			user.clear_alert( "asleep" );
			((dynamic)user).heal_overall_damage( ((dynamic)user).getBruteLoss(), ((dynamic)user).getFireLoss(), 0 );
			user.reagents.clear_reagents();

			if ( user is Mob_Living_Carbon_Human ) {
				H = user;
				((dynamic)H).restore_blood();
				((dynamic)H).remove_all_embedded_objects();
			}
			user.WriteMsg( "<span class='notice'>We have regenerated.</span>" );
			user.mind.changeling.purchasedpowers.Remove( this );
			GlobalFuncs.feedback_add_details( "changeling_powers", "CR" );
			((dynamic)user).updatehealth();
			return 1;
		}

	}

}