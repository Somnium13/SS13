// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Weapon_AiModule_Zeroth : Obj_Item_Weapon_AiModule {

		public Obj_Item_Weapon_AiModule_Zeroth ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: AI_modules.dm
		public override dynamic transmitInstructions( Game_Data law_datum = null, dynamic sender = null ) {
			dynamic failedlaw = null;
			dynamic templaw = null;

			
			if ( Lang13.Bool( ((dynamic)law_datum).owner ) ) {
				
				if ( Lang13.Bool( ((dynamic)law_datum).owner.laws.zeroth ) ) {
					((dynamic)law_datum).owner.WriteMsg( "" + sender.real_name + " attempted to modify your zeroth law." );
					((dynamic)law_datum).owner.WriteMsg( "It would be in your best interest to play along with " + sender.real_name + " that:" );

					foreach (dynamic _a in Lang13.Enumerate( this.laws )) {
						failedlaw = _a;
						
						((dynamic)law_datum).owner.WriteMsg( "" + failedlaw );
					}
					return 1;
				}
			}

			foreach (dynamic _b in Lang13.Enumerate( this.laws )) {
				templaw = _b;
				

				if ( Lang13.Bool( ((dynamic)law_datum).owner ) ) {
					((Mob_Living_Silicon)((dynamic)law_datum).owner).set_zeroth_law( templaw );
				} else {
					((AiLaws)law_datum).set_zeroth_law( templaw );
				}
			}
			return null;
		}

	}

}