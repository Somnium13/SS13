// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Weapon_Disk_Botany : Obj_Item_Weapon_Disk {

		public ByTable genes = new ByTable();
		public string genesource = "unknown";

		protected override void __FieldInit() {
			base.__FieldInit();

			this.w_class = 1;
			this.icon = "icons/obj/hydroponics.dmi";
			this.icon_state = "disk";
		}

		// Function from file: seed_machines.dm
		public Obj_Item_Weapon_Disk_Botany ( dynamic loc = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			this.pixel_x = Rand13.Int( -5, 5 );
			this.pixel_y = Rand13.Int( -5, 5 );
			return;
		}

		// Function from file: seed_machines.dm
		public override dynamic attack_self( dynamic user = null, dynamic flag = null, bool? emp = null ) {
			string choice = null;

			
			if ( this.genes.len != 0 ) {
				choice = Interface13.Alert( user, "Are you sure you want to wipe the disk?", "Xenobotany Data", "No", "Yes" );

				if ( this != null && Lang13.Bool( user ) && this.genes != null && Lang13.Bool( choice ) && choice == "Yes" && ((Mob)user).get_active_hand() == this ) {
					GlobalFuncs.to_chat( user, "You wipe the disk data." );
					this.name = Lang13.Initial( this, "name" );
					this.desc = Lang13.Initial( this, "name" );
					this.genes = new ByTable();
					this.genesource = "unknown";
				}
			}
			return null;
		}

	}

}