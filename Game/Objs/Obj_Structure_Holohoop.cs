// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Structure_Holohoop : Obj_Structure {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.anchored = 1;
			this.throwpass = true;
			this.icon = "icons/obj/basketball.dmi";
			this.icon_state = "hoop";
		}

		public Obj_Structure_Holohoop ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: HolodeckControl.dm
		public override bool CanPass( dynamic mover = null, dynamic target = null, double? height = null, bool? air_group = null ) {
			height = height ?? 1.5;
			air_group = air_group ?? false;

			dynamic I = null;

			
			if ( mover is Obj_Item && mover.throwing != 0 ) {
				I = mover;

				if ( I is Obj_Item_Weapon_Dummy || I is Obj_Item_Projectile ) {
					return false;
				}

				if ( Rand13.PercentChance( 50 ) ) {
					I.loc = this.loc;
					this.visible_message( new Txt( "<span class='notice'>Swish! " ).the( I ).item().str( " lands in " ).the( this ).item().str( ".</span>" ).ToString() );
				} else {
					this.visible_message( new Txt( "<span class='warning'>" ).the( I ).item().str( " bounces off of " ).the( this ).item().str( "'s rim!</span>" ).ToString() );
				}
				return false;
			} else {
				return base.CanPass( (object)(mover), (object)(target), height, air_group );
			}
		}

		// Function from file: HolodeckControl.dm
		public override dynamic attackby( dynamic a = null, dynamic b = null, dynamic c = null ) {
			dynamic G = null;

			
			if ( a is Obj_Item_Weapon_Grab && Map13.GetDistance( this, b ) < 2 ) {
				G = a;

				if ( Convert.ToDouble( G.state ) < 2 ) {
					GlobalFuncs.to_chat( b, "<span class='warning'>You need a better grip to do that!</span>" );
					return null;
				}
				G.affecting.loc = this.loc;
				((Mob)G.affecting).Weaken( 5 );
				this.visible_message( "<span class='warning'>" + G.assailant + " dunks " + G.affecting + " into the " + this + "!</span>" );
				GlobalFuncs.qdel( a );
				return null;
			} else if ( a is Obj_Item && Map13.GetDistance( this, b ) < 2 ) {
				
				if ( Lang13.Bool( b.drop_item( a, this.loc ) ) ) {
					this.visible_message( "<span class='notice'>" + b + " dunks " + a + " into the " + this + "!</span>" );
					return null;
				}
			}
			return null;
		}

	}

}