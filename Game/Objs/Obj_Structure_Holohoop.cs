// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Structure_Holohoop : Obj_Structure {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.anchored = 1;
			this.icon = "icons/obj/basketball.dmi";
			this.icon_state = "hoop";
		}

		public Obj_Structure_Holohoop ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: items.dm
		public override bool CanPass( dynamic mover = null, dynamic target = null, double? height = null, bool? air_group = null ) {
			height = height ?? 0;

			dynamic I = null;

			
			if ( mover is Obj_Item && mover.throwing ) {
				I = mover;

				if ( I is Obj_Item_Projectile ) {
					return false;
				}

				if ( Rand13.PercentChance( 50 ) ) {
					I.loc = this.loc;
					this.visible_message( new Txt( "<span class='warning'> Swish! " ).the( I ).item().str( " lands in " ).the( this ).item().str( ".</span>" ).ToString(), 3 );
				} else {
					this.visible_message( new Txt( "<span class='danger'> " ).the( I ).item().str( " bounces off of " ).the( this ).item().str( "'s rim!</span>" ).ToString(), 3 );
				}
				return false;
			} else {
				return base.CanPass( (object)(mover), (object)(target), height, air_group );
			}
		}

		// Function from file: items.dm
		public override dynamic attackby( dynamic A = null, dynamic user = null, string _params = null, bool? silent = null, bool? replace_spent = null ) {
			dynamic G = null;

			
			if ( A is Obj_Item_Weapon_Grab && Map13.GetDistance( this, user ) < 2 ) {
				G = A;

				if ( Convert.ToDouble( G.state ) < 2 ) {
					user.WriteMsg( "<span class='warning'>You need a better grip to do that!</span>" );
					return null;
				}
				G.affecting.loc = this.loc;
				((Mob)G.affecting).Weaken( 5 );
				this.visible_message( new Txt( "<span class='danger'>" ).item( G.assailant ).str( " dunks " ).item( G.affecting ).str( " into " ).the( this ).item().str( "!</span>" ).ToString(), 3 );
				GlobalFuncs.qdel( A );
				return null;
			} else if ( A is Obj_Item && Map13.GetDistance( this, user ) < 2 ) {
				user.drop_item( this );
				this.visible_message( new Txt( "<span class='warning'> " ).item( user ).str( " dunks " ).item( A ).str( " into " ).the( this ).item().str( "!</span>" ).ToString(), 3 );
				return null;
			}
			return null;
		}

	}

}