// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Device_Multitool : Obj_Item_Device {

		public dynamic buffer = null;
		public bool v_clone = false;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.force = 5;
			this.w_class = 2;
			this.throwforce = 5;
			this.throw_range = 15;
			this.throw_speed = 3;
			this.starting_materials = new ByTable().Set( "$iron", 50 ).Set( "$glass", 20 );
			this.w_type = 5;
			this.melt_temperature = 1687;
			this.origin_tech = "magnets=1;engineering=1";
			this.icon_state = "multitool";
		}

		public Obj_Item_Device_Multitool ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: multitool.dm
		public override dynamic examine( dynamic user = null, string size = null ) {
			dynamic _default = null;

			_default = base.examine( (object)(user), size );
			GlobalFuncs.to_chat( user, "<span class='notice'>Cloning is " + ( this.v_clone ? "enabled" : "disabled" ) + ".</span>" );
			return _default;
		}

		// Function from file: multitool.dm
		public override dynamic attack_self( dynamic user = null, dynamic flag = null, bool? emp = null ) {
			
			if ( !Lang13.Bool( this.buffer ) && !this.v_clone ) {
				return null;
			}
			this.v_clone = !this.v_clone;

			if ( this.v_clone ) {
				GlobalFuncs.to_chat( user, new Txt( "<span class='notice'>You enable cloning on " ).the( this ).item().str( ".</span>" ).ToString() );
			} else {
				GlobalFuncs.to_chat( user, new Txt( "<span class='notice'>You disable cloning on " ).the( this ).item().str( ".</span>" ).ToString() );
			}
			return null;
		}

		// Function from file: multitool.dm
		public dynamic IsBufferA( dynamic typepath = null ) {
			
			if ( !Lang13.Bool( this.buffer ) ) {
				return 0;
			}
			return typepath.IsInstanceOfType( this.buffer );
		}

	}

}