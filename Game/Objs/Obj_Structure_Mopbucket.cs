// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Structure_Mopbucket : Obj_Structure {

		public string lockedby = "";
		public int amount_per_transfer_from_this = 5;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.pressure_resistance = 5;
			this.flags = 4352;
			this.icon = "icons/obj/janitor.dmi";
			this.icon_state = "mopbucket";
		}

		// Function from file: mop_bucket.dm
		public Obj_Structure_Mopbucket ( dynamic loc = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			this.create_reagents( 100 );
			return;
		}

		// Function from file: mop_bucket.dm
		public override bool ex_act( double? severity = null, dynamic child = null ) {
			
			switch ((int?)( severity )) {
				case 1:
					GlobalFuncs.qdel( this );
					return false;
					break;
				case 2:
					
					if ( Rand13.PercentChance( 50 ) ) {
						GlobalFuncs.qdel( this );
						return false;
					}
					break;
				case 3:
					
					if ( Rand13.PercentChance( 5 ) ) {
						GlobalFuncs.qdel( this );
						return false;
					}
					break;
			}
			return false;
		}

		// Function from file: mop_bucket.dm
		public override dynamic mop_act( Obj_Item_Weapon_Mop M = null, dynamic user = null ) {
			
			if ( M is Obj_Item_Weapon_Mop ) {
				
				if ( ( this.reagents.total_volume ??0) >= 1 ) {
					
					if ( ( M.reagents.total_volume ??0) >= 25 ) {
						return 1;
					} else {
						((Reagents)this.reagents).trans_to( M, 3 );
						GlobalFuncs.to_chat( user, "<span class='notice'>You wet " + M + "</span>" );
						GlobalFuncs.playsound( GlobalFuncs.get_turf( this ), "sound/effects/slosh.ogg", 25, 1 );
					}
				} else {
					GlobalFuncs.to_chat( user, "<span class='notice'>Nothing left to wet " + M + " with!</span>" );
				}
			}
			return 1;
		}

		// Function from file: mop_bucket.dm
		public override dynamic attackby( dynamic a = null, dynamic b = null, dynamic c = null ) {
			
			if ( a is Obj_Item_Weapon_Mop ) {
				return 0;
			}
			return base.attackby( (object)(a), (object)(b), (object)(c) );
		}

		// Function from file: mop_bucket.dm
		public override dynamic attack_hand( dynamic a = null, dynamic b = null, dynamic c = null ) {
			base.attack_hand( (object)(a), (object)(b), (object)(c) );

			if ( !Lang13.Bool( this.anchored ) ) {
				this.anchored = 1;
				((Ent_Static)a).visible_message( "<span class='notice'>" + a + " locks " + this + "'s wheels!</span>" );
				this.lockedby += "[" + GlobalFuncs.time_stamp() + "] " + Task13.User + " (" + Task13.User.ckey + ") - locked " + this;
			} else {
				this.anchored = 0;
				((Ent_Static)a).visible_message( "<span class='notice'>" + a + " unlocks " + this + "'s wheels!</span>" );
				this.lockedby += "[" + GlobalFuncs.time_stamp() + "] " + Task13.User + " (" + Task13.User.ckey + ") - unlocked " + this;
			}
			return null;
		}

	}

}