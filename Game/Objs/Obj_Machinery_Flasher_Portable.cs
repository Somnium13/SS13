// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Machinery_Flasher_Portable : Obj_Machinery_Flasher {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.strength = 8;
			this.base_state = "pflash";
			this.min_harm_label = 35;
			this.icon_state = "pflash1";
		}

		public Obj_Machinery_Flasher_Portable ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: flasher.dm
		public override dynamic attackby( dynamic a = null, dynamic b = null, dynamic c = null ) {
			
			if ( a is Obj_Item_Weapon_Wrench ) {
				this.add_fingerprint( b );
				this.anchored = !Lang13.Bool( this.anchored );

				if ( !Lang13.Bool( this.anchored ) ) {
					b.show_message( "<span class='warning'>" + this + " can now be moved.</span>" );
					this.overlays.len = 0;
				} else if ( Lang13.Bool( this.anchored ) ) {
					b.show_message( "<span class='warning'>" + this + " is now secured.</span>" );
					this.overlays.Add( "" + this.base_state + "-s" );
				}
			}
			return null;
		}

		// Function from file: flasher.dm
		public override bool HasProximity( dynamic AM = null ) {
			dynamic M = null;

			
			if ( this.disable || this.last_flash != 0 && Game13.time < this.last_flash + 150 ) {
				return false;
			}

			if ( AM is Mob_Living_Carbon ) {
				M = AM;

				if ( M.m_intent != "walk" && Lang13.Bool( this.anchored ) ) {
					this.flash();
				}
			}
			return false;
		}

	}

}