// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Structure_Closet_SecureCloset_Freezer : Obj_Structure_Closet_SecureCloset {

		public Obj_Structure_Closet_SecureCloset_Freezer ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: freezer.dm
		public override bool ex_act( double? severity = null, dynamic child = null ) {
			ByTable bombs = null;

			bombs = this.search_contents_for( typeof(Obj_Item_Device_TransferValve) );

			if ( !GlobalFuncs.isemptylist( bombs ) ) {
				base.ex_act( severity, (object)(child) );
			}
			return false;
		}

		// Function from file: freezer.dm
		public override bool? update_icon( dynamic location = null, dynamic target = null ) {
			this.overlays.len = 0;

			if ( this.broken ) {
				this.icon_state = this.icon_broken;
			} else if ( !this.opened ) {
				
				if ( this.locked ) {
					this.icon_state = this.icon_locked;
				} else {
					this.icon_state = this.icon_closed;
				}

				if ( this.welded ) {
					this.overlays.Add( "welded" );
				}
			} else {
				this.icon_state = this.icon_opened;
			}
			return null;
		}

	}

}