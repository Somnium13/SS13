// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Device_Rcd_Borg : Obj_Item_Device_Rcd {

		public int cell_power_per_energy = 30;

		public Obj_Item_Device_Rcd_Borg ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: RCD.dm
		public override double get_energy( dynamic user = null ) {
			dynamic R = null;

			
			if ( !( user is Mob_Living_Silicon_Robot ) ) {
				return 0;
			}
			R = user;

			if ( !Lang13.Bool( R.cell ) ) {
				return 0;
			}
			return Convert.ToDouble( R.cell.charge / this.cell_power_per_energy );
		}

		// Function from file: RCD.dm
		public override void use_energy( double amount = 0, dynamic user = null ) {
			dynamic R = null;

			
			if ( !( user is Mob_Living_Silicon_Robot ) ) {
				return;
			}
			R = user;

			if ( !Lang13.Bool( R.cell ) ) {
				return;
			}
			R.cell.use( amount * this.cell_power_per_energy );
			return;
		}

		// Function from file: RCD.dm
		public override dynamic attack_self( dynamic user = null, dynamic flag = null, bool? emp = null ) {
			
			if ( !( this.selected != null ) || user.shown_schematics_background || !this.selected.show( user ) ) {
				user.hud_used.toggle_show_schematics_display( this.schematics["Construction"], false, this );
			}
			return null;
		}

	}

}