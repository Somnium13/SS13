// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Weapon_Rcs : Obj_Item_Weapon {

		public int rcharges = 10;
		public dynamic pad = null;
		public int last_charge = 30;
		public bool mode = false;
		public bool rand_x = false;
		public bool rand_y = false;
		public bool emagged = false;
		public bool teleporting = false;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.flags = 64;
			this.force = 10;
			this.throwforce = 10;
			this.throw_range = 5;
			this.icon = "icons/obj/telescience.dmi";
			this.icon_state = "rcs";
		}

		// Function from file: telepad.dm
		public Obj_Item_Weapon_Rcs ( dynamic loc = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			GlobalVars.SSobj.processing.Or( this );
			return;
		}

		// Function from file: telepad.dm
		public override bool emag_act( dynamic user = null ) {
			EffectSystem_SparkSpread s = null;

			
			if ( !this.emagged ) {
				this.emagged = true;
				s = new EffectSystem_SparkSpread();
				s.set_up( 5, 1, this );
				s.start();
				user.WriteMsg( "<span class='caution'>You emag the RCS. Click on it to toggle between modes.</span>" );
			}
			return false;
		}

		// Function from file: telepad.dm
		public override dynamic attack_self( dynamic user = null, dynamic flag = null, bool? emp = null ) {
			
			if ( this.emagged ) {
				
				if ( !this.mode ) {
					this.mode = true;
					GlobalFuncs.playsound( this.loc, "sound/effects/pop.ogg", 50, 0 );
					user.WriteMsg( "<span class='caution'>The telepad locator has become uncalibrated.</span>" );
				} else {
					this.mode = false;
					GlobalFuncs.playsound( this.loc, "sound/effects/pop.ogg", 50, 0 );
					user.WriteMsg( "<span class='caution'>You calibrate the telepad locator.</span>" );
				}
			}
			return null;
		}

		// Function from file: telepad.dm
		public override int? process( dynamic seconds = null ) {
			
			if ( this.rcharges > 10 ) {
				this.rcharges = 10;
			}

			if ( this.last_charge == 0 ) {
				this.rcharges++;
				this.last_charge = 30;
			} else {
				this.last_charge--;
			}
			return null;
		}

		// Function from file: telepad.dm
		public override dynamic Destroy(  ) {
			GlobalVars.SSobj.processing.Remove( this );
			return base.Destroy();
		}

		// Function from file: telepad.dm
		public override double examine( dynamic user = null ) {
			base.examine( (object)(user) );
			user.WriteMsg( new Txt( "There are " ).item( this.rcharges ).str( " charge" ).s().str( " left." ).ToString() );
			return 0;
		}

	}

}