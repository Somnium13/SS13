// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Clothing_Suit_Clownpiece_Flying : Obj_Item_Clothing_Suit_Clownpiece {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.icon_state = "clownpiece-fly";
		}

		public Obj_Item_Clothing_Suit_Clownpiece_Flying ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: miscellaneous.dm
		public override dynamic dropped( dynamic user = null ) {
			
			if ( user.flying ) {
				user.flying = false;
				Icon13.Animate( new ByTable().Set( 1, user ).Set( "pixel_y", this.pixel_y + 10 ).Set( "time", 1 ).Set( "loop", 1 ) );
				Icon13.Animate( new ByTable().Set( 1, user ).Set( "pixel_y", this.pixel_y ).Set( "time", 10 ).Set( "loop", 1 ).Set( "easing", 1 ) );
				Icon13.Animate( user );

				if ( user.lying == true ) {
					user.pixel_y -= 6;
				}
			}
			base.dropped( (object)(user) );
			return null;
		}

		// Function from file: miscellaneous.dm
		public override dynamic equipped( dynamic user = null, dynamic slot = null ) {
			dynamic H = null;

			H = user;

			if ( !( H is Mob_Living_Carbon_Human ) ) {
				return null;
			}

			if ( slot == 13 && !user.flying ) {
				user.flying = true;
				Icon13.Animate( new ByTable().Set( 1, user ).Set( "pixel_y", this.pixel_y + 10 ).Set( "time", 10 ).Set( "loop", 1 ).Set( "easing", 1 ) );
			}
			return null;
		}

		// Function from file: miscellaneous.dm
		public override dynamic attack_hand( dynamic a = null, dynamic b = null, dynamic c = null ) {
			
			if ( !( a is Mob_Living_Carbon_Human ) ) {
				return base.attack_hand( (object)(a), (object)(b), (object)(c) );
			}

			if ( this == a.wear_suit && a.flying ) {
				a.flying = false;
				Icon13.Animate( new ByTable().Set( 1, a ).Set( "pixel_y", this.pixel_y + 10 ).Set( "time", 1 ).Set( "loop", 1 ) );
				Icon13.Animate( new ByTable().Set( 1, a ).Set( "pixel_y", this.pixel_y ).Set( "time", 10 ).Set( "loop", 1 ).Set( "easing", 1 ) );
				Icon13.Animate( a );

				if ( a.lying == true ) {
					a.pixel_y -= 6;
				}
			}
			base.attack_hand( (object)(a), (object)(b), (object)(c) );
			return null;
		}

	}

}