// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Machinery_Gashapon : Obj_Machinery {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.anchored = 1;
			this.machine_flags = 24;
			this.icon = "icons/obj/gashapon.dmi";
			this.icon_state = "gashapon";
		}

		public Obj_Machinery_Gashapon ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: gashapon.dm
		public override dynamic attackby( dynamic a = null, dynamic b = null, dynamic c = null ) {
			Obj_Item_Weapon_Capsule b2 = null;
			dynamic real_coin = null;

			
			if ( GlobalFuncs.is_type_in_list( a, new ByTable(new object [] { typeof(Obj_Item_Weapon_Coin), typeof(Obj_Item_Weapon_ReagentContainers_Food_Snacks_Chococoin) }) ) ) {
				
				if ( Lang13.Bool( b.drop_item( a, this ) ) ) {
					((Ent_Static)b).visible_message( "<span class='notice'>" + b + " puts a coin into " + this + " and turns the knob.</span>", "<span class='notice'>You put a coin into " + this + " and turn the knob.</span>" );
					this.visible_message( "<span class='notice'>" + this + " clicks softly.</span>" );
					Task13.Sleep( Rand13.Int( 10, 15 ) );
					this.visible_message( "<span class='notice'>" + this + " dispenses a capsule!</span>" );
					b2 = new Obj_Item_Weapon_Capsule( this.loc );
					b2.icon_state = "capsule" + Rand13.Int( 1, 12 );

					if ( a is Obj_Item_Weapon_Coin ) {
						real_coin = a;

						if ( real_coin.string_attached == true ) {
							
							if ( Rand13.PercentChance( 30 ) ) {
								GlobalFuncs.to_chat( b, "<SPAN CLASS='notice'>You were able to force the knob around and successfully pulled the coin out before " + this + " could swallow it.</SPAN>" );
								((Mob)b).put_in_hands( a );
							} else {
								GlobalFuncs.to_chat( b, "<SPAN CLASS='notice'>You weren't able to pull the coin out fast enough, the machine ate it, string and all.</SPAN>" );
								GlobalFuncs.qdel( a );
							}
						}
					} else {
						GlobalFuncs.qdel( a );
					}
				}
			} else if ( a is Obj_Item_Weapon_ReagentContainers_Food_Snacks_Customizable_Candy_Coin ) {
				GlobalFuncs.to_chat( b, "<span class='rose'>That coin is smudgy and oddly soft, you don't think that would work.</span>" );
				return null;
			} else {
				return base.attackby( (object)(a), (object)(b), (object)(c) );
			}
			return null;
		}

	}

}