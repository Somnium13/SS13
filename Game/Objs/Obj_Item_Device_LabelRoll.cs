// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Device_LabelRoll : Obj_Item_Device {

		public int? left = 250;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.w_class = 1;
			this.icon = "icons/obj/bureaucracy.dmi";
			this.icon_state = "label_cart";
		}

		// Function from file: handlabeler.dm
		public Obj_Item_Device_LabelRoll ( dynamic loc = null, int? amount = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;

			if ( Lang13.Bool( amount ) ) {
				this.left = amount;
			}
			return;
		}

		// Function from file: handlabeler.dm
		public override dynamic examine( dynamic user = null, string size = null ) {
			base.examine( (object)(user), size );

			if ( Lang13.Bool( this.left ) ) {
				GlobalFuncs.to_chat( user, "<span class='info'>There " + ( ( this.left ??0) > 1 ? "are " + this.left + " letters" : "is one letter" ) + " worth of label on the roll.</span>" );
			} else {
				GlobalFuncs.to_chat( user, "<span class='warning'>Something has fucked up and this item should have deleted itself. Throw it away for IMMERSION.</span>" );
			}
			return null;
		}

	}

}