// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Weapon_Gun_Energy_KineticAccelerator_Cyborg : Obj_Item_Weapon_Gun_Energy_KineticAccelerator {

		public int charge_tick = 0;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.cell_type = "/obj/item/weapon/cell/miningborg";
		}

		// Function from file: special.dm
		public Obj_Item_Weapon_Gun_Energy_KineticAccelerator_Cyborg ( dynamic loc = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			GlobalVars.processing_objects.Add( this );
			return;
		}

		// Function from file: special.dm
		public override dynamic process(  ) {
			Ent_Static R = null;

			this.charge_tick++;

			if ( this.charge_tick < 3 ) {
				return 0;
			}
			this.charge_tick = 0;

			if ( !Lang13.Bool( this.power_supply ) ) {
				return 0;
			}

			if ( this.loc is Mob_Living_Silicon_Robot ) {
				R = this.loc;

				if ( R != null && Lang13.Bool( ((dynamic)R).cell ) ) {
					((dynamic)R).cell.use( this.charge_cost );
					((Obj_Item_Weapon_Cell)this.power_supply).give( this.charge_cost );
				}
			}
			this.update_icon();
			return 1;
		}

		// Function from file: special.dm
		public override dynamic Destroy( dynamic brokenup = null ) {
			GlobalVars.processing_objects.Remove( this );
			base.Destroy( (object)(brokenup) );
			return null;
		}

	}

}