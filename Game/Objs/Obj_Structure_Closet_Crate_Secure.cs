// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Structure_Closet_Crate_Secure : Obj_Structure_Closet_Crate {

		public string redlight = "securecrater";
		public string greenlight = "securecrateg";
		public string sparks = "securecratesparks";
		public string emag = "securecrateemag";

		protected override void __FieldInit() {
			base.__FieldInit();

			this.icon_opened = "securecrateopen";
			this.icon_closed = "securecrate";
			this.locked = true;
			this.health = 1000;
			this.icon_state = "securecrate";
		}

		// Function from file: crates.dm
		public Obj_Structure_Closet_Crate_Secure ( dynamic loc = null ) : base( (object)(loc) ) {
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;

			if ( this.locked ) {
				this.overlays.len = 0;
				this.overlays.Add( this.redlight );
			} else {
				this.overlays.len = 0;
				this.overlays.Add( this.greenlight );
			}
			return;
		}

		// Function from file: crates.dm
		public override dynamic emp_act( int severity = 0 ) {
			Obj O = null;

			
			foreach (dynamic _a in Lang13.Enumerate( this, typeof(Obj) )) {
				O = _a;
				
				O.emp_act( severity );
			}

			if ( !this.broken && !this.opened && Rand13.PercentChance( ((int)( 50 / severity )) ) ) {
				
				if ( !this.locked ) {
					this.locked = true;
					this.overlays.len = 0;
					this.overlays.Add( this.redlight );
				} else {
					this.overlays.len = 0;
					this.overlays.Add( this.emag );
					this.overlays.Add( this.sparks );
					Task13.Schedule( 6, (Task13.Closure)(() => {
						this.overlays.Remove( this.sparks );
						return;
					}));
					GlobalFuncs.playsound( GlobalFuncs.get_turf( this ), "sound/effects/sparks4.ogg", 75, 1 );
					this.locked = false;
				}
			}

			if ( !this.opened && Rand13.PercentChance( ((int)( 20 / severity )) ) ) {
				
				if ( !this.locked ) {
					this.open();
				} else {
					this.req_access = new ByTable();
					this.req_access += Rand13.PickFromTable( GlobalFuncs.get_all_accesses() );
				}
			}
			base.emp_act( severity );
			return null;
		}

		// Function from file: crates.dm
		public override dynamic attackby( dynamic a = null, dynamic b = null, dynamic c = null ) {
			
			if ( a is Obj_Item_Weapon_Card && this.allowed( b ) && !this.locked && !this.opened && !this.broken ) {
				GlobalFuncs.to_chat( b, new Txt( "<span class='notice'>You lock " ).the( this ).item().str( ".</span>" ).ToString() );
				this.locked = true;
				this.overlays.len = 0;
				this.overlays.Add( this.redlight );
				return null;
			} else if ( a is Obj_Item_Weapon_Card_Emag && this.locked && !this.broken ) {
				this.overlays.len = 0;
				this.overlays.Add( this.emag );
				this.overlays.Add( this.sparks );
				Task13.Schedule( 6, (Task13.Closure)(() => {
					this.overlays.Remove( this.sparks );
					return;
				}));
				GlobalFuncs.playsound( GlobalFuncs.get_turf( this ), "sparks", 60, 1 );
				this.locked = false;
				this.broken = true;
				GlobalFuncs.to_chat( b, new Txt( "<span class='notice'>You unlock " ).the( this ).item().str( ".</span>" ).ToString() );
				return null;
			}
			return base.attackby( (object)(a), (object)(b), (object)(c) );
		}

		// Function from file: crates.dm
		public override dynamic attack_hand( dynamic a = null, dynamic b = null, dynamic c = null ) {
			
			if ( !this.Adjacent( a ) ) {
				return null;
			}

			if ( this.locked && !this.broken ) {
				
				if ( this.allowed( a ) ) {
					GlobalFuncs.to_chat( a, "<span class='notice'>You unlock " + this + ".</span>" );
					this.locked = false;
					this.overlays.len = 0;
					this.overlays.Add( this.greenlight );
					return null;
				} else {
					GlobalFuncs.to_chat( a, "<span class='notice'>" + this + " is locked.</span>" );
					return null;
				}
			} else {
				base.attack_hand( (object)(a), (object)(b), (object)(c) );
			}
			return null;
		}

	}

}