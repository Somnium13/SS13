// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Structure_Sign_Poster : Obj_Structure_Sign {

		public dynamic serial_number = null;
		public bool ruined = false;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.icon = "icons/obj/contraband.dmi";
		}

		// Function from file: poster.dm
		public Obj_Structure_Sign_Poster ( dynamic serial = null ) : base( (object)(serial) ) {
			dynamic designtype = null;
			dynamic design = null;

			this.serial_number = serial;

			if ( this.serial_number == -1 ) {
				this.name += "Award of Sufficiency";
				this.desc += "The mere sight of it makes you very proud.";
				this.icon_state = "goldstar";
			} else {
				
				if ( this.serial_number == this.loc ) {
					this.serial_number = Rand13.Int( 1, GlobalVars.poster_designs.len );
				}
				designtype = GlobalVars.poster_designs[this.serial_number];
				design = Lang13.Call( designtype );
				this.name += " - " + design.name;
				this.desc += " " + design.desc;
				this.icon_state = design.icon_state;
				base();
			}
			return;
		}

		// Function from file: poster.dm
		public void roll_and_drop( Ent_Static newloc = null ) {
			
			if ( newloc != null ) {
				new Obj_Item_Mounted_Poster( newloc, this.serial_number );
			} else {
				new Obj_Item_Mounted_Poster( GlobalFuncs.get_turf( this ), this.serial_number );
			}
			GlobalFuncs.qdel( this );
			return;
		}

		// Function from file: poster.dm
		public override dynamic attack_hand( dynamic a = null, dynamic b = null, dynamic c = null ) {
			Ent_Static temp_loc = null;

			
			if ( this.ruined ) {
				return null;
			}
			temp_loc = a.loc;

			switch ((string)( Interface13.Alert( "Do I want to rip the poster from the wall?", "You think...", "Yes", "No" ) )) {
				case "Yes":
					
					if ( a.loc != temp_loc ) {
						return null;
					}
					this.visible_message( "<span class='warning'>" + a + " rips " + this + " in a single, decisive motion!</span>" );
					GlobalFuncs.playsound( GlobalFuncs.get_turf( this ), "sound/items/poster_ripped.ogg", 100, 1 );
					this.ruined = true;
					this.icon_state = "poster_ripped";
					this.name = "ripped poster";
					this.desc = "You can't make out anything from the poster's original print. It's ruined.";
					this.add_fingerprint( a );
					break;
				case "No":
					return null;
					break;
			}
			return null;
		}

		// Function from file: poster.dm
		public override dynamic attackby( dynamic a = null, dynamic b = null, dynamic c = null ) {
			
			if ( a is Obj_Item_Weapon_Wirecutters ) {
				GlobalFuncs.playsound( this.loc, "sound/items/Wirecutter.ogg", 100, 1 );

				if ( this.ruined ) {
					GlobalFuncs.to_chat( b, "<span class='notice'>You remove the remnants of the poster.</span>" );
					GlobalFuncs.qdel( this );
				} else {
					GlobalFuncs.to_chat( b, "<span class='notice'>You carefully remove the poster from the wall.</span>" );
					this.roll_and_drop( b.loc );
				}
				return null;
			}
			return null;
		}

	}

}