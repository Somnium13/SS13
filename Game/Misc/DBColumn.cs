// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class DBColumn : Game_Data {

		public dynamic name = null;
		public dynamic table = null;
		public dynamic position = null;
		public dynamic sql_type = null;
		public dynamic flags = null;
		public dynamic length = null;
		public dynamic max_length = null;

		// Function from file: core.dm
		public DBColumn ( dynamic name_handler = null, dynamic table_handler = null, dynamic position_handler = null, dynamic type_handler = null, dynamic flag_handler = null, dynamic length_handler = null, dynamic max_length_handler = null ) {
			this.name = name_handler;
			this.table = table_handler;
			this.position = position_handler;
			this.sql_type = type_handler;
			this.flags = flag_handler;
			this.length = length_handler;
			this.max_length = max_length_handler;
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			return;
		}

		// Function from file: core.dm
		public string SqlTypeName( dynamic type_handler = null ) {
			type_handler = type_handler ?? this.sql_type;

			
			dynamic _a = type_handler; // Was a switch-case, sorry for the mess.
			if ( _a==1 ) {
				return "TINYINT";
			} else if ( _a==2 ) {
				return "SMALLINT";
			} else if ( _a==3 ) {
				return "MEDIUMINT";
			} else if ( _a==4 ) {
				return "INTEGER";
			} else if ( _a==5 ) {
				return "BIGINT";
			} else if ( _a==7 ) {
				return "FLOAT";
			} else if ( _a==8 ) {
				return "DOUBLE";
			} else if ( _a==9 ) {
				return "DATE";
			} else if ( _a==10 ) {
				return "DATETIME";
			} else if ( _a==11 ) {
				return "TIMESTAMP";
			} else if ( _a==12 ) {
				return "TIME";
			} else if ( _a==13 ) {
				return "STRING";
			} else if ( _a==14 ) {
				return "BLOB";
			}
			return null;
		}

	}

}