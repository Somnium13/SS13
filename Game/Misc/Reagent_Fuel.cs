// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Fuel : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Welding fuel";
			this.id = "fuel";
			this.description = "Required for welders. Flamable.";
			this.reagent_state = 2;
			this.color = "#660000";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}
			M.adjustToxLoss( 1 );
			return false;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool reaction_turf( dynamic T = null, double volume = 0 ) {
			
			if ( base.reaction_turf( (object)(T), volume ) ) {
				return true;
			}

			if ( !Lang13.Bool( Lang13.FindIn( typeof(Obj_Effect_Decal_Cleanable_LiquidFuel), T ) ) ) {
				GlobalFuncs.getFromPool( typeof(Obj_Effect_Decal_Cleanable_LiquidFuel), T, volume );
			}
			return false;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool reaction_obj( dynamic O = null, double volume = 0 ) {
			Reagent_Fuel self = null;
			dynamic T = null;

			self = this;

			if ( base.reaction_obj( (object)(O), volume ) ) {
				return true;
			}
			T = GlobalFuncs.get_turf( O );
			self.reaction_turf( T, volume );
			return false;
		}

	}

}