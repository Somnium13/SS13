// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Brig : Objective {

		public bool already_completed = false;

		public Objective_Brig ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override dynamic check_completion(  ) {
			
			if ( this.blocked ) {
				return 0;
			}

			if ( this.already_completed ) {
				return 1;
			}

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				
				if ( Convert.ToInt32( this.target.current.stat ) == 2 ) {
					return 0;
				}

				if ( ((Mind)this.target).is_brigged( 3000 ) ) {
					this.already_completed = true;
					return 1;
				}
				return 0;
			}
			return 0;
		}

		// Function from file: objective.dm
		public override dynamic find_target_by_role( dynamic role = null, bool? role_type = null ) {
			role_type = role_type ?? false;

			base.find_target_by_role( (object)(role), role_type );

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = "Have " + this.target.current.real_name + ", the " + ( !( role_type == true ) ? this.target.assigned_role : this.target.special_role ) + " brigged for 10 minutes.";
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

		// Function from file: objective.dm
		public override dynamic find_target(  ) {
			base.find_target();

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = "Have " + this.target.current.real_name + ", the " + this.target.assigned_role + " brigged for 10 minutes.";
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

	}

}