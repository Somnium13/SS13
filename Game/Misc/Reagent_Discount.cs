// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Discount : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Discount Dan's Special Sauce";
			this.id = "discount";
			this.description = "You can almost feel your liver failing, just by looking at it.";
			this.reagent_state = 2;
			this.color = "#6F884F";
			this.data = 1;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			Mob_Living H = null;
			dynamic L = null;

			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}

			if ( M is Mob_Living_Carbon_Human ) {
				H = M;

				dynamic _a = this.volume; // Was a switch-case, sorry for the mess.
				if ( 1<=_a&&_a<=20 ) {
					
					if ( Rand13.PercentChance( 5 ) ) {
						H.WriteMsg( "<span class='warning'>You don't feel very good.</span>" );
						((dynamic)this.holder).remove_reagent( this.id, 0.04 );
					}
				} else if ( 20<=_a&&_a<=35 ) {
					
					if ( Rand13.PercentChance( 10 ) ) {
						H.WriteMsg( "<span class='warning'>You really don't feel very good.</span>" );
					}

					if ( Rand13.PercentChance( 5 ) ) {
						H.adjustToxLoss( 0.1 );
						H.visible_message( "" + H + " groans." );
						((dynamic)this.holder).remove_reagent( this.id, 0.12 );
					}
				} else if ( 35<=_a&&_a<=Double.PositiveInfinity ) {
					
					if ( Rand13.PercentChance( 10 ) ) {
						H.WriteMsg( "<span class='warning'>Your stomach grumbles unsettlingly.</span>" );
					}

					if ( Rand13.PercentChance( 5 ) ) {
						H.WriteMsg( "<span class='warning'>Something feels wrong with your body.</span>" );
						L = ((dynamic)H).internal_organs_by_name["liver"];

						if ( L is Organ_Internal_Liver ) {
							L.take_damage( 0.1, 1 );
						}
						H.adjustToxLoss( 0.21 );
						((dynamic)this.holder).remove_reagent( this.id, 0.2 );
					}
				}
			}
			return false;
		}

	}

}