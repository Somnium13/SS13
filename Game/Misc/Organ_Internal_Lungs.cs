// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Organ_Internal_Lungs : Organ_Internal {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "lungs";
			this.removed_type = typeof(Obj_Item_Organ_Lungs);
		}

		public Organ_Internal_Lungs ( Mob_Living_Carbon_Human H = null ) : base( H ) {
			
		}

		// Function from file: organ_internal.dm
		public override bool process(  ) {
			base.process();

			if ( this.germ_level > 100 ) {
				
				if ( Rand13.PercentChance( 5 ) ) {
					this.owner.emote( "cough" );
				}
			}

			if ( this.is_bruised() ) {
				
				if ( Rand13.PercentChance( 2 ) ) {
					Task13.Schedule( 0, (Task13.Closure)(() => {
						this.owner.emote( "me", 1, "coughs up blood!" );
						return;
					}));
					this.owner.drip( 10 );
				}

				if ( Rand13.PercentChance( 4 ) ) {
					Task13.Schedule( 0, (Task13.Closure)(() => {
						this.owner.emote( "me", 1, "gasps for air!" );
						return;
					}));
					this.owner.losebreath += 5;
				}
			}
			return false;
		}

	}

}