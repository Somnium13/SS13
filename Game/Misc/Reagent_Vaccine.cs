// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Vaccine : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Vaccine";
			this.id = "vaccine";
			this.reagent_state = 2;
			this.color = "#C81040";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool reaction_mob( dynamic M = null, int? method = null, double volume = 0 ) {
			method = method ?? GlobalVars.TOUCH;

			Reagent_Vaccine self = null;
			Disease D = null;
			Disease A = null;

			self = this;

			if ( base.reaction_mob( (object)(M), method, volume ) ) {
				return true;
			}

			if ( Lang13.Bool( self.data ) && method == GlobalVars.INGEST ) {
				
				foreach (dynamic _a in Lang13.Enumerate( M.viruses, typeof(Disease) )) {
					D = _a;
					

					if ( D is Disease_Advance ) {
						A = D;

						if ( ((dynamic)A).GetDiseaseID() == self.data ) {
							D.f_cure();
						}
					} else if ( D.type == self.data ) {
						D.f_cure();
					}
				}
				M.resistances.Add( self.data );
			}
			return false;
		}

	}

}