// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Impedrezene : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Impedrezene";
			this.id = "impedrezene";
			this.description = "Impedrezene is a narcotic that impedes one's ability by slowing down the higher brain cell functions.";
			this.reagent_state = 2;
			this.color = "#C8A5DC";
			this.overdose = 30;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}
			M.jitteriness = Num13.MaxInt( M.jitteriness - 5, 0 );

			if ( Rand13.PercentChance( 80 ) ) {
				M.adjustBrainLoss( 2.5 );
			}

			if ( Rand13.PercentChance( 50 ) ) {
				M.drowsyness = Num13.MaxInt( Convert.ToInt32( M.drowsyness ), 3 );
			}

			if ( Rand13.PercentChance( 10 ) ) {
				M.emote( "drool" );
			}
			return false;
		}

	}

}