// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class LogController : Game_Data {

		public string subject = "";
		public string filename = null;
		public File handle = null;

		// Function from file: admin_investigate.dm
		public LogController ( string sub = null, bool? persist = null, string filename = null ) {
			persist = persist ?? GlobalVars.FALSE;

			this.subject = sub;
			this.filename = "" + "data/investigate/" + this.subject + ".html";

			if ( !( filename == null ) ) {
				this.filename = filename;
			}

			if ( !( persist == true ) ) {
				File13.Delete( this.filename );
			}
			this.handle = new File( this.filename );
			return;
		}

		// Function from file: admin_investigate.dm
		public void read( Mob user = null ) {
			Interface13.Browse( user, this.handle, "window=investigate" + this.subject + ";size=800x300" );
			return;
		}

		// Function from file: admin_investigate.dm
		public void write( string message = null ) {
			GlobalFuncs.to_chat( this.handle, String13.UrlDecode( message ) );
			return;
		}

	}

}