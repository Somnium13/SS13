// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class SurgeryStep_Head_Peel : SurgeryStep_Head {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.allowed_tools = new ByTable().Set( typeof(Obj_Item_Weapon_Retractor), 100 ).Set( typeof(Obj_Item_Weapon_Crowbar), 75 ).Set( typeof(Obj_Item_Weapon_Kitchen_Utensil_Fork), 50 );
			this.min_duration = 80;
			this.max_duration = 100;
		}

		// Function from file: headreattach.dm
		public override bool? fail_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			Organ_External affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );

			if ( affected.parent != null ) {
				affected = affected.parent;
				((Ent_Static)user).visible_message( "<span class='warning'>" + user + "'s hand slips, ripping " + target + "'s " + affected.display_name + " open!</span>", "<span class='warning'>Your hand slips,  ripping " + target + "'s " + affected.display_name + " open!</span>" );
				affected.createwound( "cut", 10 );
			}
			return null;
		}

		// Function from file: headreattach.dm
		public override bool end_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			dynamic affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );
			((Ent_Static)user).visible_message( new Txt( "<span class='notice'>" ).item( user ).str( " peels back tattered flesh where " ).item( target ).str( "'s head used to be with " ).the( tool ).item().str( ".</span>" ).ToString(), new Txt( "<span class='notice'>You peel back tattered flesh where " ).item( target ).str( "'s head used to be with " ).the( tool ).item().str( ".</span>" ).ToString() );
			affected.status |= 1;
			return false;
		}

		// Function from file: headreattach.dm
		public override bool begin_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			((Ent_Static)user).visible_message( new Txt().item( user ).str( " starts peeling back tattered flesh where " ).item( target ).str( "'s head used to be with " ).the( tool ).item().str( "." ).ToString(), new Txt( "You start peeling back tattered flesh where " ).item( target ).str( "'s head used to be with " ).the( tool ).item().str( "." ).ToString() );
			base.begin_step( (object)(user), (object)(target), target_zone, tool, (object)(surgery) );
			return false;
		}

	}

}