// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Disease_Brainrot : Disease {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Brainrot";
			this.max_stages = 4;
			this.spread = "On contact";
			this.spread_type = 4;
			this.cure = "Alkysine";
			this.cure_id = new ByTable(new object [] { "alkysine" });
			this.agent = "Cryptococcus Cosmosis";
			this.affected_species = new ByTable(new object [] { "Human" });
			this.cure_chance = 15;
			this.desc = "This disease destroys the braincells, causing brain fever, brain necrosis and general intoxication.";
			this.severity = "Major";
		}

		public Disease_Brainrot ( bool? process = null, Disease_Advance D = null ) : base( process, D ) {
			
		}

		// Function from file: brainrot.dm
		public override bool stage_act(  ) {
			dynamic O = null;

			base.stage_act();

			switch ((int?)( this.stage )) {
				case 2:
					
					if ( Rand13.PercentChance( 2 ) ) {
						((Mob)this.affected_mob).emote( "blink" );
					}

					if ( Rand13.PercentChance( 2 ) ) {
						((Mob)this.affected_mob).emote( "yawn" );
					}

					if ( Rand13.PercentChance( 2 ) ) {
						GlobalFuncs.to_chat( this.affected_mob, "<span class='warning'>Your don't feel like yourself.</span>" );
					}

					if ( Rand13.PercentChance( 5 ) ) {
						((Mob_Living)this.affected_mob).adjustBrainLoss( 1 );
						((Mob_Living)this.affected_mob).updatehealth();
					}
					break;
				case 3:
					
					if ( Rand13.PercentChance( 2 ) ) {
						((Mob)this.affected_mob).emote( "stare" );
					}

					if ( Rand13.PercentChance( 2 ) ) {
						((Mob)this.affected_mob).emote( "drool" );
					}

					if ( Rand13.PercentChance( 10 ) && ((Mob_Living)this.affected_mob).getBrainLoss() <= 98 ) {
						((Mob_Living)this.affected_mob).adjustBrainLoss( 2 );
						((Mob_Living)this.affected_mob).updatehealth();

						if ( Rand13.PercentChance( 2 ) ) {
							GlobalFuncs.to_chat( this.affected_mob, "<span class='warning'>Your try to remember something important...but can't.</span>" );
						}
					}
					break;
				case 4:
					
					if ( Rand13.PercentChance( 2 ) ) {
						((Mob)this.affected_mob).emote( "stare" );
					}

					if ( Rand13.PercentChance( 2 ) ) {
						((Mob)this.affected_mob).emote( "drool" );
					}

					if ( Rand13.PercentChance( 15 ) && ((Mob_Living)this.affected_mob).getBrainLoss() <= 98 ) {
						((Mob_Living)this.affected_mob).adjustBrainLoss( 3 );
						((Mob_Living)this.affected_mob).updatehealth();

						if ( Rand13.PercentChance( 2 ) ) {
							GlobalFuncs.to_chat( this.affected_mob, "<span class='warning'>Strange buzzing fills your head, removing all thoughts.</span>" );
						}
					}

					if ( Rand13.PercentChance( 3 ) ) {
						GlobalFuncs.to_chat( this.affected_mob, "<span class='warning'>You lose consciousness...</span>" );

						foreach (dynamic _a in Lang13.Enumerate( Map13.FetchViewers( null, this.affected_mob ) )) {
							O = _a;
							
							((Mob)O).show_message( "" + this.affected_mob + " suddenly collapses", 1 );
						}
						((Mob)this.affected_mob).Paralyse( Rand13.Int( 5, 10 ) );

						if ( Rand13.PercentChance( 1 ) ) {
							((Mob)this.affected_mob).emote( "snore" );
						}
					}

					if ( Rand13.PercentChance( 15 ) ) {
						this.affected_mob.stuttering += 3;
					}
					break;
			}
			return false;
		}

	}

}