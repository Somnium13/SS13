// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Disease2_Effect_Scc : Disease2_Effect {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Spontaneous Cellular Collapse";
		}

		// Function from file: effect.dm
		public override bool activate( Mob_Living mob = null, bool multiplier = false ) {
			Mob_Living H = null;
			Organ_External E = null;

			
			if ( !( mob is Mob_Living_Carbon_Human ) ) {
				return false;
			}
			H = mob;
			((Reagents)mob.reagents).add_reagent( "pacid", 10 );
			GlobalFuncs.to_chat( mob, "<span class = 'warning'>Your body burns as your cells break down.</span>" );
			GlobalFuncs.shake_camera( mob, ( multiplier ?1:0) * 5 );

			foreach (dynamic _a in Lang13.Enumerate( ((dynamic)H).organs, typeof(Organ_External) )) {
				E = _a;
				

				if ( Lang13.Bool( Rand13.Pick(new object [] { 1, 0 }) ) ) {
					E.createwound( "cut", Lang13.DoubleNullable( Rand13.Pick(new object [] { 2, 4, 6, 8, 10 }) ) );
					E.fracture();
				}
			}
			return false;
		}

	}

}