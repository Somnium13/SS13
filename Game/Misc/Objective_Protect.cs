// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Protect : Objective {

		public Objective_Protect ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override dynamic check_completion(  ) {
			
			if ( this.blocked ) {
				return 0;
			}

			if ( !Lang13.Bool( this.target ) ) {
				return 1;
			}

			if ( Lang13.Bool( this.target.current ) ) {
				
				if ( Convert.ToInt32( this.target.current.stat ) == 2 || this.target.current is Mob_Living_Silicon || this.target.current is Mob_Living_Carbon_Brain || this.target.current is Mob_Living_SimpleAnimal_Borer ) {
					return 0;
				}
				return 1;
			}
			return 0;
		}

		// Function from file: objective.dm
		public override dynamic find_target_by_role( dynamic role = null, bool? role_type = null ) {
			role_type = role_type ?? false;

			base.find_target_by_role( (object)(role), role_type );

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = "Protect " + this.target.current.real_name + ", the " + ( !( role_type == true ) ? this.target.assigned_role : this.target.special_role ) + ".";
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

		// Function from file: objective.dm
		public override dynamic find_target(  ) {
			base.find_target();

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = "Protect " + this.target.current.real_name + ", the " + this.target.assigned_role + ".";
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

	}

}