// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Construction_MechaChassis : Construction {

		public Construction_MechaChassis ( Ent_Static atom = null ) : base( atom ) {
			
		}

		// Function from file: mecha_construction_chassis.dm
		public override bool action( dynamic used_atom = null, dynamic user = null ) {
			return this.check_all_steps( used_atom, user );
		}

		// Function from file: mecha_construction_chassis.dm
		public override bool custom_action( dynamic index = null, dynamic diff = null, dynamic used_atom = null, dynamic user = null ) {
			((Ent_Static)used_atom).visible_message( "" + used_atom + " has connected " + diff + " to " + this.holder + ".", "You connect " + diff + " to " + this.holder );
			this.holder.overlays.Add( diff.icon_state + "+o" );
			GlobalFuncs.qdel( diff );
			diff = null;
			return true;
		}

	}

}