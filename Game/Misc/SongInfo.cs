// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class SongInfo : Game_Data {

		public string title = "";
		public string artist = "";
		public string album = "";
		public string url = "";
		public double? length = 0;
		public bool emagged = false;

		// Function from file: jukebox.dm
		public SongInfo ( ByTable json = null ) {
			this.title = json["title"];
			this.artist = json["artist"];
			this.album = json["album"];
			this.url = json["url"];
			this.length = String13.ParseNumber( json["length"] );
			return;
		}

		// Function from file: jukebox.dm
		public string displaytitle(  ) {
			string str = null;

			
			if ( this.artist == "" && this.title == "" ) {
				return "[NO TAGS]";
			}
			str = "";

			if ( this.artist != "" ) {
				str += this.artist + " - ";
			}

			if ( this.title != "" ) {
				str += "\"" + this.title + "\"";
			} else {
				str += "Untitled";
			}

			if ( this.album != "" && this.artist == "" ) {
				str += " (" + this.album + ")";
			}
			return str;
		}

		// Function from file: jukebox.dm
		public string display(  ) {
			string str = null;

			str = "\"" + this.title + "\"";

			if ( this.artist != "" ) {
				str += ", by " + this.artist;
			}

			if ( this.album != "" ) {
				str += ", from '" + this.album + "'";
			}
			return str;
		}

	}

}