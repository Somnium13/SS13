// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_SpaceDrugs : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Space drugs";
			this.id = "space_drugs";
			this.description = "An illegal chemical compound used as drug.";
			this.reagent_state = 2;
			this.color = "#60A584";
			this.custom_metabolism = 0.5;
			this.overdose = 30;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}
			M.druggy = Num13.MaxInt( M.druggy, 15 );

			if ( M.loc is Tile && !( M.loc is Tile_Space ) ) {
				
				if ( M.canmove && !M.restrained() ) {
					
					if ( Rand13.PercentChance( 10 ) ) {
						Map13.Step( M, Convert.ToInt32( Rand13.PickFromTable( GlobalVars.cardinal ) ) );
					}
				}
			}

			if ( Rand13.PercentChance( 7 ) ) {
				M.emote( Rand13.Pick(new object [] { "twitch", "drool", "moan", "giggle" }) );
			}
			return false;
		}

	}

}