// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class ConnectionManager : Game_Data {

		public Connection N = null;
		public Connection S = null;
		public Connection E = null;
		public Connection W = null;

		// Function from file: ConnectionManager.dm
		public bool check( Connection c = null ) {
			return c != null && c.valid();
		}

		// Function from file: ConnectionManager.dm
		public void erase_all(  ) {
			
			if ( this.check( this.N ) ) {
				this.N.erase();
			}

			if ( this.check( this.S ) ) {
				this.S.erase();
			}

			if ( this.check( this.E ) ) {
				this.E.erase();
			}

			if ( this.check( this.W ) ) {
				this.W.erase();
			}
			return;
		}

		// Function from file: ConnectionManager.dm
		public void update_all(  ) {
			
			if ( this.check( this.N ) ) {
				this.N.update();
			}

			if ( this.check( this.S ) ) {
				this.S.update();
			}

			if ( this.check( this.E ) ) {
				this.E.update();
			}

			if ( this.check( this.W ) ) {
				this.W.update();
			}
			return;
		}

		// Function from file: ConnectionManager.dm
		public void place( Connection c = null, int d = 0 ) {
			
			switch ((int)( d )) {
				case 1:
					this.N = c;
					break;
				case 2:
					this.S = c;
					break;
				case 4:
					this.E = c;
					break;
				case 8:
					this.W = c;
					break;
			}
			return;
		}

		// Function from file: ConnectionManager.dm
		public Connection get( int d = 0 ) {
			
			switch ((int)( d )) {
				case 1:
					
					if ( this.check( this.N ) ) {
						return this.N;
					} else {
						return null;
					}
					break;
				case 2:
					
					if ( this.check( this.S ) ) {
						return this.S;
					} else {
						return null;
					}
					break;
				case 4:
					
					if ( this.check( this.E ) ) {
						return this.E;
					} else {
						return null;
					}
					break;
				case 8:
					
					if ( this.check( this.W ) ) {
						return this.W;
					} else {
						return null;
					}
					break;
			}
			return null;
		}

	}

}