// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class GlobalIterator_MechaIntertialMovement : GlobalIterator {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.delay = 7;
		}

		public GlobalIterator_MechaIntertialMovement ( ByTable arguments = null, bool? autostart = null ) : base( arguments, autostart ) {
			
		}

		// Function from file: mecha.dm
		public override bool process( Obj port = null, dynamic mecha = null ) {
			
			if ( Lang13.Bool( mecha ) ) {
				Map13.Step( port, Convert.ToInt32( mecha ) );

				if ( !false || ((Obj_Mecha)port).check_for_support() ) {
					this.stop();
				}
			} else {
				this.stop();
			}
			return false;
		}

	}

}