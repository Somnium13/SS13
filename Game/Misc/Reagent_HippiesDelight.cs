// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_HippiesDelight : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Hippie's Delight";
			this.id = "hippiesdelight";
			this.description = "You just don't get it maaaan.";
			this.reagent_state = 2;
			this.color = "#664300";
			this.data = 1;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}
			M.druggy = Num13.MaxInt( M.druggy, 50 );

			dynamic _a = this.data; // Was a switch-case, sorry for the mess.
			if ( 1<=_a&&_a<=5 ) {
				
				if ( !Lang13.Bool( M.stuttering ) ) {
					M.stuttering = 1;
				}
				M.Dizzy( 10 );

				if ( Rand13.PercentChance( 10 ) ) {
					M.emote( Rand13.Pick(new object [] { "twitch", "giggle" }) );
				}
			} else if ( 5<=_a&&_a<=10 ) {
				
				if ( !Lang13.Bool( M.stuttering ) ) {
					M.stuttering = 1;
				}
				M.Jitter( 20 );
				M.Dizzy( 20 );
				M.druggy = Num13.MaxInt( M.druggy, 45 );

				if ( Rand13.PercentChance( 20 ) ) {
					M.emote( Rand13.Pick(new object [] { "twitch", "giggle" }) );
				}
			} else if ( 10<=_a&&_a<=Double.PositiveInfinity ) {
				
				if ( !Lang13.Bool( M.stuttering ) ) {
					M.stuttering = 1;
				}
				M.Jitter( 40 );
				M.Dizzy( 40 );
				M.druggy = Num13.MaxInt( M.druggy, 60 );

				if ( Rand13.PercentChance( 30 ) ) {
					M.emote( Rand13.Pick(new object [] { "twitch", "giggle" }) );
				}
			}
			this.data++;
			return false;
		}

	}

}