// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Adminordrazine : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Adminordrazine";
			this.id = "adminordrazine";
			this.description = "It's magic. We don't have to explain it.";
			this.reagent_state = 2;
			this.color = "#C8A5DC";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			Disease D = null;

			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}
			M.setCloneLoss( 0 );
			M.setOxyLoss( 0 );
			M.radiation = 0;
			M.heal_organ_damage( 5, 5 );
			M.adjustToxLoss( -5 );

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "toxin" ) ) ) {
				((dynamic)this.holder).remove_reagent( "toxin", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "stoxin" ) ) ) {
				((dynamic)this.holder).remove_reagent( "stoxin", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "plasma" ) ) ) {
				((dynamic)this.holder).remove_reagent( "plasma", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "sacid" ) ) ) {
				((dynamic)this.holder).remove_reagent( "sacid", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "pacid" ) ) ) {
				((dynamic)this.holder).remove_reagent( "pacid", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "cyanide" ) ) ) {
				((dynamic)this.holder).remove_reagent( "cyanide", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "lexorin" ) ) ) {
				((dynamic)this.holder).remove_reagent( "lexorin", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "amatoxin" ) ) ) {
				((dynamic)this.holder).remove_reagent( "amatoxin", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "chloralhydrate" ) ) ) {
				((dynamic)this.holder).remove_reagent( "chloralhydrate", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "carpotoxin" ) ) ) {
				((dynamic)this.holder).remove_reagent( "carpotoxin", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "zombiepowder" ) ) ) {
				((dynamic)this.holder).remove_reagent( "zombiepowder", 5 );
			}

			if ( Lang13.Bool( ((dynamic)this.holder).has_reagent( "mindbreaker" ) ) ) {
				((dynamic)this.holder).remove_reagent( "mindbreaker", 5 );
			}
			M.hallucination = 0;
			M.setBrainLoss( 0 );
			M.disabilities = 0;
			M.sdisabilities = 0;
			M.eye_blurry = 0;
			M.eye_blind = 0;
			M.SetWeakened( 0 );
			M.SetStunned( false );
			M.SetParalysis( 0 );
			M.silent = 0;
			M.dizziness = 0;
			M.drowsyness = 0;
			M.stuttering = 0;
			M.confused = 0;
			M.sleeping = 0;
			M.jitteriness = 0;

			foreach (dynamic _a in Lang13.Enumerate( M.viruses, typeof(Disease) )) {
				D = _a;
				
				D.spread = "Remissive";
				D.stage--;

				if ( ( D.stage ??0) < 1 ) {
					D.f_cure();
				}
			}
			return false;
		}

		// Function from file: hydroponics_reagents.dm
		public override void on_plant_life( Obj_Machinery_PortableAtmospherics_Hydroponics T = null ) {
			base.on_plant_life( T );
			T.adjust_nutrient( 1 );
			T.adjust_water( 1 );
			T.weedlevel -= 5;
			T.pestlevel -= 5;
			T.toxins -= 5;

			if ( T.seed != null && !T.dead ) {
				T.health += 50;
			}
			return;
		}

	}

}