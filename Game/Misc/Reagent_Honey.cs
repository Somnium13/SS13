// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Honey : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Honey";
			this.id = "honey";
			this.description = "A golden yellow syrup, loaded with sugary sweetness.";
			this.color = "#FEAE00";
			this.alpha = 200;
			this.nutriment_factor = 3;
		}

		// Function from file: bees_items.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			Mob_Living H = null;

			
			if ( M is Mob_Living_Carbon_Human ) {
				H = M;

				if ( !( this.holder != null ) ) {
					return false;
				}
				H.nutrition += this.nutriment_factor;
				((dynamic)this.holder).remove_reagent( this.id, 0.4 );

				if ( H.getBruteLoss() != 0 && Rand13.PercentChance( 60 ) ) {
					H.heal_organ_damage( 2, 0 );
				}

				if ( H.getFireLoss() != 0 && Rand13.PercentChance( 50 ) ) {
					H.heal_organ_damage( 0, 2 );
				}

				if ( Lang13.Bool( H.getToxLoss() ) && Rand13.PercentChance( 50 ) ) {
					H.adjustToxLoss( -2 );
				}
				base.on_mob_life( M, alien );
				return false;
			}
			return false;
		}

	}

}