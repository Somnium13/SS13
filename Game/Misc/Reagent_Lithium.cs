// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Lithium : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Lithium";
			this.id = "lithium";
			this.description = "A chemical element, used as antidepressant.";
			this.color = "#808080";
			this.overdose = 30;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}

			if ( M.canmove && !M.restrained() && M.loc is Tile_Space ) {
				Map13.Step( M, Convert.ToInt32( Rand13.PickFromTable( GlobalVars.cardinal ) ) );
			}

			if ( Rand13.PercentChance( 5 ) ) {
				M.emote( Rand13.Pick(new object [] { "twitch", "drool", "moan" }) );
			}
			return false;
		}

	}

}