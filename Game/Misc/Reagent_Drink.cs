// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Drink : Reagent {

		public int adj_dizzy = 0;
		public int adj_drowsy = 0;
		public int adj_sleepy = 0;
		public int adj_temp = 0;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Drink";
			this.id = "drink";
			this.description = "Uh, some kind of drink.";
			this.reagent_state = 2;
			this.nutriment_factor = 0.2;
			this.color = "#E78108";
			this.custom_metabolism = 0.4;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}
			M.nutrition += this.nutriment_factor * 0.2 / 0.4;

			if ( this.adj_dizzy != 0 ) {
				M.dizziness = Num13.MaxInt( 0, M.dizziness + this.adj_dizzy );
			}

			if ( this.adj_drowsy != 0 ) {
				M.drowsyness = Num13.MaxInt( 0, Convert.ToInt32( M.drowsyness + this.adj_drowsy ) );
			}

			if ( this.adj_sleepy != 0 ) {
				M.sleeping = Num13.MaxInt( 0, M.sleeping + this.adj_sleepy );
			}

			if ( this.adj_temp != 0 ) {
				
				if ( Convert.ToDouble( M.bodytemperature ) < 310 ) {
					M.bodytemperature = Num13.MinInt( 310, Convert.ToInt32( M.bodytemperature + 37.5 ) );
				}
			}
			return false;
		}

	}

}