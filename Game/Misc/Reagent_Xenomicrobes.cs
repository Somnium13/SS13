// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Xenomicrobes : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Xenomicrobes";
			this.id = "xenomicrobes";
			this.description = "Microbes with an entirely alien cellular structure.";
			this.reagent_state = 2;
			this.color = "#535E66";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool reaction_mob( dynamic M = null, int? method = null, double volume = 0 ) {
			method = method ?? GlobalVars.TOUCH;

			
			if ( base.reaction_mob( (object)(M), method, volume ) ) {
				return true;
			}

			if ( Rand13.PercentChance( 10 ) && method == GlobalVars.TOUCH || method == GlobalVars.INGEST ) {
				((Mob)M).contract_disease( new Disease_XenoTransformation( false ), true );
			}
			return false;
		}

	}

}