// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class SurgeryStep_Generic_CutOpen : SurgeryStep_Generic {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.allowed_tools = new ByTable().Set( typeof(Obj_Item_Weapon_Scalpel), 100 ).Set( typeof(Obj_Item_Weapon_Kitchen_Utensil_Knife_Large), 75 ).Set( typeof(Obj_Item_Weapon_Shard), 50 );
			this.min_duration = 90;
			this.max_duration = 110;
		}

		// Function from file: generic.dm
		public override bool? fail_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			Organ_External affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );
			((Ent_Static)user).visible_message( new Txt( "<span class='warning'>" ).item( user ).str( "'s hand slips, slicing open " ).item( target ).str( "'s " ).item( affected.display_name ).str( " in the wrong place with " ).the( tool ).item().str( "!</span>" ).ToString(), new Txt( "<span class='warning'>Your hand slips, slicing open " ).item( target ).str( "'s " ).item( affected.display_name ).str( " in the wrong place with " ).the( tool ).item().str( "!</span>" ).ToString() );
			affected.createwound( "cut", 10 );
			return null;
		}

		// Function from file: generic.dm
		public override bool end_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			dynamic affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );
			((Ent_Static)user).visible_message( new Txt( "<span class='notice'>" ).item( user ).str( " has made an incision on " ).item( target ).str( "'s " ).item( affected.display_name ).str( " with " ).the( tool ).item().str( ".</span>" ).ToString(), new Txt( "<span class='notice'>You have made an incision on " ).item( target ).str( "'s " ).item( affected.display_name ).str( " with " ).the( tool ).item().str( ".</span>" ).ToString() );
			affected.open = 1;
			affected.status |= 8;
			((Organ_External)affected).createwound( "cut", 1 );
			return false;
		}

		// Function from file: generic.dm
		public override bool begin_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			dynamic affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );
			((Ent_Static)user).visible_message( new Txt().item( user ).str( " starts the incision on " ).item( target ).str( "'s " ).item( affected.display_name ).str( " with " ).the( tool ).item().str( "." ).ToString(), new Txt( "You start the incision on " ).item( target ).str( "'s " ).item( affected.display_name ).str( " with " ).the( tool ).item().str( "." ).ToString() );
			((Mob_Living_Carbon_Human)target).custom_pain( "You feel a horrible pain as if from a sharp knife in your " + affected.display_name + "!", true );
			base.begin_step( (object)(user), (object)(target), target_zone, tool, (object)(surgery) );
			return false;
		}

		// Function from file: generic.dm
		public override int can_use( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null ) {
			int _default = 0;

			dynamic affected = null;

			_default = base.can_use( (object)(user), (object)(target), target_zone, tool );

			if ( Lang13.Bool( target.species ) && Lang13.Bool( target.species.flags & 32768 ) ) {
				GlobalFuncs.to_chat( user, "<span class='info'>" + target + " has no skin!</span>" );
				return 0;
			}
			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );

			if ( _default != 0 && !Lang13.Bool( affected.open ) && target_zone != "mouth" ) {
				return _default;
			}
			return 0;
		}

		// Function from file: generic.dm
		public override bool tool_quality( Obj_Item tool = null ) {
			bool _default = false;

			_default = base.tool_quality( tool );

			if ( !Lang13.Bool( tool.is_sharp() ) ) {
				return false;
			}
			return _default;
		}

	}

}