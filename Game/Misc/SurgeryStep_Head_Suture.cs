// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class SurgeryStep_Head_Suture : SurgeryStep_Head {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.allowed_tools = new ByTable().Set( typeof(Obj_Item_Weapon_Hemostat), 100 ).Set( typeof(Obj_Item_Stack_CableCoil), 60 ).Set( typeof(Obj_Item_Weapon_FixOVein), 80 );
			this.min_duration = 80;
			this.max_duration = 100;
		}

		// Function from file: headreattach.dm
		public override bool? fail_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			string affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );

			if ( Lang13.Bool( ((dynamic)affected).parent ) ) {
				affected = ((dynamic)affected).parent;
				((Ent_Static)user).visible_message( "<span class='warning'>" + user + "'s hand slips, ripping apart flesh on " + target + "'s neck!</span>", "<span class='warning'>Your hand slips, ripping apart flesh on " + target + "'s neck!</span>" );
				((Mob_Living)target).apply_damage( 10, "brute", affected );
			}
			return null;
		}

		// Function from file: headreattach.dm
		public override bool end_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			dynamic affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );
			((Ent_Static)user).visible_message( new Txt( "<span class='notice'>" ).item( user ).str( " has finished stapling " ).item( target ).str( "'s neck into place with " ).the( tool ).item().str( ".</span>" ).ToString(), new Txt( "<span class='notice'>You have finished stapling " ).item( target ).str( "'s neck into place with " ).the( tool ).item().str( ".</span>" ).ToString() );
			affected.open = 4;
			return false;
		}

		// Function from file: headreattach.dm
		public override bool begin_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			((Ent_Static)user).visible_message( new Txt().item( user ).str( " is stapling and suturing flesh into place in " ).item( target ).str( "'s esophagal and vocal region with " ).the( tool ).item().str( "." ).ToString(), new Txt( "You start to staple and suture flesh into place in " ).item( target ).str( "'s esophagal and vocal region with " ).the( tool ).item().str( "." ).ToString() );
			base.begin_step( (object)(user), (object)(target), target_zone, tool, (object)(surgery) );
			return false;
		}

		// Function from file: headreattach.dm
		public override int can_use( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null ) {
			dynamic affected = null;

			affected = ((Mob_Living_Carbon_Human)target).get_organ( target_zone );
			return base.can_use( (object)(user), (object)(target), target_zone, tool ) != 0 && Convert.ToInt32( affected.open ) == 3 ?1:0;
		}

	}

}