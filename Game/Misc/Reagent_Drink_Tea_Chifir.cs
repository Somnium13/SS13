// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Drink_Tea_Chifir : Reagent_Drink_Tea {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Chifir";
			this.id = "chifir";
			this.description = "Strong Russian tea, it'll help you remember what you had for lunch!";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			Mob_Living H = null;
			Reagent reagent = null;

			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}

			if ( M is Mob_Living_Carbon_Human && Rand13.PercentChance( 5 ) ) {
				H = M;
				((dynamic)H).vomit();
			}

			foreach (dynamic _a in Lang13.Enumerate( ((dynamic)this.holder).reagent_list, typeof(Reagent) )) {
				reagent = _a;
				

				if ( GlobalVars.chifir_doesnt_remove.Contains( reagent.id ) ) {
					continue;
				}
				((dynamic)this.holder).remove_reagent( reagent.id, 1.5 );
			}
			M.adjustToxLoss( -1 );
			return false;
		}

	}

}