// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Blood : Objective {

		public Objective_Blood ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override dynamic check_completion(  ) {
			
			if ( this.blocked ) {
				return 0;
			}

			if ( this.owner != null && Lang13.Bool( ((dynamic)this.owner).vampire ) && ((dynamic)this.owner).vampire.bloodtotal != 0 && ((dynamic)this.owner).vampire.bloodtotal >= Convert.ToDouble( this.target_amount ) ) {
				return 1;
			} else {
				return 0;
			}
		}

		// Function from file: objective.dm
		public dynamic gen_amount_goal( int? low = null, int? high = null ) {
			low = low ?? 150;
			high = high ?? 400;

			this.target_amount = Rand13.Int( low ??0, high ??0 );
			this.target_amount = Num13.Floor( Num13.Floor( Convert.ToDouble( this.target_amount / 5 ) ) * 5 );
			this.explanation_text = "Accumulate atleast " + this.target_amount + " units of blood in total.";
			return this.target_amount;
		}

	}

}