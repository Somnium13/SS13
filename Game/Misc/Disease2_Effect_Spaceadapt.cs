// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Disease2_Effect_Spaceadapt : Disease2_Effect {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Space Adaptation Effect";
			this.stage = 5;
		}

		// Function from file: effect.dm
		public override bool activate( Mob_Living mob = null, bool multiplier = false ) {
			Mob_Living H = null;
			dynamic chest = null;
			Organ_Internal I = null;

			H = mob;

			if ( ( ((Reagents)mob.reagents).get_reagent_amount( "dexalinp" ) ?1:0) < 10 ) {
				((Reagents)mob.reagents).add_reagent( "dexalinp", 4 );
			}

			if ( ( ((Reagents)mob.reagents).get_reagent_amount( "leporazine" ) ?1:0) < 10 ) {
				((Reagents)mob.reagents).add_reagent( "leporazine", 4 );
			}

			if ( ( ((Reagents)mob.reagents).get_reagent_amount( "bicaridine" ) ?1:0) < 10 ) {
				((Reagents)mob.reagents).add_reagent( "bicaridine", 4 );
			}

			if ( ( ((Reagents)mob.reagents).get_reagent_amount( "dermaline" ) ?1:0) < 10 ) {
				((Reagents)mob.reagents).add_reagent( "dermaline", 4 );
			}
			mob.emote( "me", 1, "exhales slowly." );

			if ( H is Mob_Living_Carbon_Human ) {
				chest = ((Mob_Living_Carbon_Human)H).get_organ( "chest" );

				foreach (dynamic _a in Lang13.Enumerate( chest.internal_organs, typeof(Organ_Internal) )) {
					I = _a;
					
					I.damage = 0;
				}
			}
			return false;
		}

	}

}