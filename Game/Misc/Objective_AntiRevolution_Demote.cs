// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_AntiRevolution_Demote : Objective_AntiRevolution {

		public Objective_AntiRevolution_Demote ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override dynamic check_completion(  ) {
			Obj_Item_Weapon_Card_Id I = null;
			Obj_Item_Weapon_Card_Id P = null;

			
			if ( this.blocked ) {
				return 0;
			}

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) && this.target is Mob_Living_Carbon_Human ) {
				I = this.target.current.wear_id;

				if ( I is Obj_Item_Device_Pda ) {
					P = I;
					I = ((dynamic)P).id;
				}

				if ( !( I is Obj_Item_Weapon_Card_Id ) ) {
					return 1;
				}

				if ( I.assignment == "Assistant" ) {
					return 1;
				} else {
					return 0;
				}
			}
			return 1;
		}

		// Function from file: objective.dm
		public override dynamic find_target_by_role( dynamic role = null, bool? role_type = null ) {
			role_type = role_type ?? false;

			base.find_target_by_role( (object)(role), role_type );

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = new Txt().item( this.target.current.real_name ).str( ", the " ).item( ( !( role_type == true ) ? this.target.assigned_role : this.target.special_role ) ).str( " has been classified as harmful to Nanotrasen's goals. Demote " ).him_her_it_them().item( this.target.current ).str( " to assistant." ).ToString();
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

		// Function from file: objective.dm
		public override dynamic find_target(  ) {
			base.find_target();

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = new Txt().item( this.target.current.real_name ).str( ", the " ).item( this.target.assigned_role ).str( "  has been classified as harmful to Nanotrasen's goals. Demote " ).him_her_it_them().item( this.target.current ).str( " to assistant." ).ToString();
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

	}

}