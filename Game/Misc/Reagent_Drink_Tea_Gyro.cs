// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Drink_Tea_Gyro : Reagent_Drink_Tea {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Gyro";
			this.id = "gyro";
			this.description = "Nyo ho ho~";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			int prev_dir = 0;
			dynamic i = null;
			Mob_Living H = null;
			dynamic zone = null;

			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}

			if ( Rand13.PercentChance( 30 ) ) {
				M.emote( "spin" );
			}
			prev_dir = M.dir;
			M.confused++;

			foreach (dynamic _a in Lang13.Enumerate( new ByTable(new object [] { 1, 4, 2, 8, 1, 4, 2, 8, 1, 4, 2, 8, 1, 4, 2, 8 }) )) {
				i = _a;
				
				M.dir = Convert.ToInt32( i );
				Task13.Sleep( 1 );
			}
			M.dir = prev_dir;

			if ( M is Mob_Living_Carbon_Human ) {
				H = M;

				foreach (dynamic _b in Lang13.Enumerate( new ByTable(new object [] { "l_leg", "r_leg", "l_foot", "r_foot" }) )) {
					zone = _b;
					
					((dynamic)H).HealDamage( zone, Rand13.Int( 1, 3 ), Rand13.Int( 1, 3 ) );
				}
			}
			return false;
		}

	}

}