// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Zombiepowder : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Zombie Powder";
			this.id = "zombiepowder";
			this.description = "A strong neurotoxin that puts the subject into a death-like state.";
			this.color = "#669900";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_removal( dynamic data = null ) {
			int newvol = 0;
			Mob M = null;

			
			if ( !base.on_removal( (object)(data) ) ) {
				return false;
			}
			newvol = Num13.MaxInt( 0, ((int)( ( this.volume ??0) - Convert.ToDouble( data ) )) );

			if ( ((dynamic)this.holder).my_atom is Mob_Living_Carbon ) {
				M = ((dynamic)this.holder).my_atom;

				if ( newvol >= 1 ) {
					M.status_flags |= 8192;
				} else {
					M.status_flags &= 57343;
				}
			}
			return true;
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}

			if ( ( this.volume ??0) >= 1 ) {
				M.status_flags |= 8192;
			} else {
				M.status_flags &= 57343;
			}
			M.adjustOxyLoss( 0.25 );
			M.adjustToxLoss( 0.25 );
			M.Weaken( 10 );
			M.silent = Num13.MaxInt( M.silent, 10 );
			M.tod = GlobalFuncs.worldtime2text();
			return false;
		}

	}

}