// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Silence : Objective {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.explanation_text = "Do not allow anyone to escape the station.  Only allow the shuttle to be called when everyone is dead and your story is the only one left.";
		}

		public Objective_Silence ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override dynamic check_completion(  ) {
			Mob_Living player = null;
			dynamic T = null;

			
			if ( this.blocked ) {
				return 0;
			}

			if ( GlobalVars.emergency_shuttle.location < 2 ) {
				return 0;
			}

			foreach (dynamic _b in Lang13.Enumerate( GlobalVars.player_list, typeof(Mob_Living) )) {
				player = _b;
				

				if ( player == ((dynamic)this.owner).current ) {
					continue;
				}

				if ( player.mind != null ) {
					
					if ( player.stat != 2 ) {
						T = GlobalFuncs.get_turf( player );

						if ( !Lang13.Bool( T ) ) {
							continue;
						}

						if (
							T.loc.type == typeof(Zone_Shuttle_Escape_Centcom) ||
							T.loc.type == typeof(Zone_Shuttle_EscapePod1_Centcom) || 
							T.loc.type == typeof(Zone_Shuttle_EscapePod2_Centcom) ||
							T.loc.type == typeof(Zone_Shuttle_EscapePod3_Centcom) ||
							T.loc.type == typeof(Zone_Shuttle_EscapePod5_Centcom)
						) {
							return 0;
						}
					}
				}
			}
			return 1;
		}

	}

}