// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class FoodProcessorProcess_Poison : FoodProcessorProcess {

		// Function from file: processor.dm
		public override void process( Ent_Static loc = null, Ent_Dynamic what = null ) {
			Ent_Dynamic M = null;

			
			if ( loc != null && what is Obj_Item_Weapon_ReagentContainers_Food_Snacks_Meat ) {
				M = what;

				if ( Lang13.Bool( ((dynamic)M).poisonsacs ) ) {
					((Ent_Dynamic)((dynamic)M).poisonsacs).forceMove( loc );
					((dynamic)M).poisonsacs = null;
					M.desc = "An excellent " + this + "!";
					((Reagents)M.reagents).del_reagent( "toxin" );
					((Reagents)M.reagents).del_reagent( "carpotoxin" );
				}
				what.forceMove( loc );
			}
			return;
		}

	}

}