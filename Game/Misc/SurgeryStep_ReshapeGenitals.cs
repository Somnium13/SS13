// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class SurgeryStep_ReshapeGenitals : SurgeryStep {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.allowed_tools = new ByTable()
				.Set( typeof(Obj_Item_Weapon_Scalpel), 100 )
				.Set( typeof(Obj_Item_Weapon_Kitchen_Utensil_Knife_Large), 75 )
				.Set( typeof(Obj_Item_Weapon_Hatchet), 50 )
				.Set( typeof(Obj_Item_Weapon_Wirecutters), 35 )
			;
			this.priority = 10;
			this.min_duration = 80;
			this.max_duration = 100;
			this.blood_level = 2;
		}

		// Function from file: genderchange.dm
		public override bool? fail_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			((Ent_Static)user).visible_message( "<span class='warning'>" + user + " mutilates " + target + "'s genitals beyond recognition!</span>" );
			((Mob_Living)target).apply_damage( 50, "brute", "groin", 1 );
			((Mob)target).emote( "scream", null, null, true );
			((Game_Data)target).setGender( Rand13.Pick(new object [] { GlobalVars.MALE, GlobalVars.FEMALE }) );
			((Mob)target).regenerate_icons();
			return true;
		}

		// Function from file: genderchange.dm
		public override bool end_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			
			if ( target.gender == GlobalVars.FEMALE ) {
				((Ent_Static)user).visible_message( "<span class='notice'>" + user + " has made a man out of " + target + "!</span>" );
				((Game_Data)target).setGender( GlobalVars.MALE );
			} else {
				((Ent_Static)user).visible_message( "<span class='notice'>" + user + " has made a woman out of " + target + "!</span>" );
				((Game_Data)target).setGender( GlobalVars.FEMALE );
			}
			((Mob)target).regenerate_icons();
			target.op_stage.genitals = false;
			return true;
		}

		// Function from file: genderchange.dm
		public override bool begin_step( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null, dynamic surgery = null ) {
			
			if ( target.gender == GlobalVars.FEMALE ) {
				((Ent_Static)user).visible_message( "<span class='notice'>" + user + " begins to reshape " + target + "'s genitals to look more masculine.</span>" );
			} else {
				((Ent_Static)user).visible_message( "<span class='notice'>" + user + " begins to reshape " + target + "'s genitals to look more feminine.</span>" );
			}
			return false;
		}

		// Function from file: genderchange.dm
		public override int can_use( dynamic user = null, dynamic target = null, string target_zone = null, Obj_Item tool = null ) {
			return target_zone == "groin" && GlobalFuncs.hasorgans( target ) && target.op_stage.genitals ?1:0;
		}

		// Function from file: genderchange.dm
		public override bool tool_quality( Obj_Item tool = null ) {
			bool _default = false;

			_default = base.tool_quality( tool );

			if ( !Lang13.Bool( tool.is_sharp() ) ) {
				return false;
			}
			return _default;
		}

	}

}