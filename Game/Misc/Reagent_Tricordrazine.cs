// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Tricordrazine : Reagent {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Tricordrazine";
			this.id = "tricordrazine";
			this.description = "Tricordrazine is a highly potent stimulant, originally derived from cordrazine. Can be used to treat a wide range of injuries.";
			this.reagent_state = 2;
			this.color = "#C8A5DC";
		}

		// Function from file: Chemistry-Reagents.dm
		public override bool on_mob_life( Mob_Living M = null, int? alien = null ) {
			
			if ( base.on_mob_life( M, alien ) ) {
				return true;
			}

			if ( Lang13.Bool( M.getOxyLoss() ) && Rand13.PercentChance( 80 ) ) {
				M.adjustOxyLoss( -0.5 );
			}

			if ( M.getBruteLoss() != 0 && Rand13.PercentChance( 80 ) ) {
				M.heal_organ_damage( 0.5, 0 );
			}

			if ( M.getFireLoss() != 0 && Rand13.PercentChance( 80 ) ) {
				M.heal_organ_damage( 0, 0.5 );
			}

			if ( Lang13.Bool( M.getToxLoss() ) && Rand13.PercentChance( 80 ) ) {
				M.adjustToxLoss( -0.5 );
			}
			return false;
		}

	}

}