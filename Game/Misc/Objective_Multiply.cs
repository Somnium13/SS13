// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Multiply : Objective {

		public bool already_completed = false;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.explanation_text = "Procreate, and protect your spawn.";
		}

		public Objective_Multiply ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override dynamic check_completion(  ) {
			Mob_Living_Silicon_Robot B = null;

			
			if ( this.blocked ) {
				return 0;
			}

			if ( this.already_completed ) {
				return 1;
			}

			if ( !Lang13.Bool( ((dynamic)this.owner).current ) ) {
				return 0;
			}
			B = ((dynamic)this.owner).current;

			if ( !( B is Mob_Living_SimpleAnimal_Borer ) ) {
				return 0;
			}

			if ( Convert.ToDouble( ((dynamic)B).numChildren ) > 0 ) {
				this.already_completed = true;
				return 1;
			}
			return 0;
		}

	}

}