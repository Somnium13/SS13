// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Symptom_Vomit : Symptom {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Vomiting";
			this.stealth = -2;
			this.resistance = -1;
			this.transmittable = 1;
			this.level = 3;
		}

		// Function from file: vomit.dm
		public virtual void Vomit( dynamic M = null ) {
			dynamic pos = null;

			((Ent_Static)M).visible_message( "<B>" + M + "</B> vomits on the floor!" );
			M.nutrition = Num13.MaxInt( ((int)( M.nutrition - 40 )), 0 );
			((Mob_Living)M).adjustToxLoss( -3 );
			pos = GlobalFuncs.get_turf( M );
			((Ent_Static)pos).add_vomit_floor( M );
			GlobalFuncs.playsound( pos, "sound/effects/splat.ogg", 50, 1 );
			return;
		}

		// Function from file: vomit.dm
		public override void Activate( Disease_Advance A = null ) {
			dynamic M = null;

			base.Activate( A );

			if ( Rand13.PercentChance( ((int)( 1.5 )) ) ) {
				M = A.affected_mob;

				switch ((int?)( A.stage )) {
					case 1:
					case 2:
					case 3:
					case 4:
						GlobalFuncs.to_chat( M, "" + Rand13.Pick(new object [] { "<span class='warning'>You feel nauseous...", "<span class='danger'>You feel like you're going to throw up!" }) + "</span>" );
						break;
					default:
						this.Vomit( M );
						break;
				}
			}
			return;
		}

	}

}