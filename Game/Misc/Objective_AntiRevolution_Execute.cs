// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_AntiRevolution_Execute : Objective_AntiRevolution {

		public Objective_AntiRevolution_Execute ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override dynamic check_completion(  ) {
			
			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				
				if ( Convert.ToInt32( this.target.current.stat ) == 2 || !( this.target.current is Mob_Living_Carbon_Human ) ) {
					return 1;
				}
				return 0;
			}
			return 1;
		}

		// Function from file: objective.dm
		public override dynamic find_target_by_role( dynamic role = null, bool? role_type = null ) {
			role_type = role_type ?? false;

			base.find_target_by_role( (object)(role), role_type );

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = new Txt().item( this.target.current.real_name ).str( ", the " ).item( ( !( role_type == true ) ? this.target.assigned_role : this.target.special_role ) ).str( " has extracted confidential information above their clearance. Execute " ).him_her_it_them().item( this.target.current ).str( "." ).ToString();
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

		// Function from file: objective.dm
		public override dynamic find_target(  ) {
			base.find_target();

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = new Txt().item( this.target.current.real_name ).str( ", the " ).item( this.target.assigned_role ).str( " has extracted confidential information above their clearance. Execute " ).him_her_it_them().item( this.target.current ).str( "." ).ToString();
			} else {
				this.explanation_text = "Free Objective";
			}
			return this.target;
		}

	}

}