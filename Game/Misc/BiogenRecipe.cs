// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class BiogenRecipe : Game_Data {

		public string id = "";
		public int cost = 0;
		public string category = "";
		public string name = "";
		public int amount_per_unit = 1;
		public ByTable other_amounts = new ByTable();
		public string reagent = null;
		public Type result = null;

		// Function from file: biogenerator.dm
		public string Render( Obj_Machinery_Biogenerator context = null ) {
			string html = null;
			bool first = false;
			dynamic amount = null;

			html = new Txt( "<li><a href='?src=" ).Ref( context ).str( ";action=create;item=" ).item( this.id ).str( ";num=1'>" ).item( ( this.amount_per_unit == 1 ? "" : "" + this.amount_per_unit + " " ) ).item( this.name ).str( "</a> <FONT COLOR=blue>(" ).item( this.cost ).str( ")</FONT>" ).ToString();

			if ( this.other_amounts.len != 0 ) {
				first = true;
				html += " (";

				foreach (dynamic _a in Lang13.Enumerate( this.other_amounts )) {
					amount = _a;
					

					if ( !first ) {
						html += " ";
					}
					html += new Txt( "<A href='?src=" ).Ref( context ).str( ";action=create;item=" ).item( this.id ).str( ";num=" ).item( amount ).str( "'>x" ).item( amount * this.amount_per_unit ).str( "</A>" ).ToString();
					first = false;
				}
				html += ")";
			}
			html += "</li>";
			return html;
		}

	}

}