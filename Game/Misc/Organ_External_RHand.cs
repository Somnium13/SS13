// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Organ_External_RHand : Organ_External {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "r_hand";
			this.display_name = "right hand";
			this.icon_name = "r_hand";
			this.max_damage = 40;
			this.min_broken_damage = 15;
			this.body_part = 1024;
			this.slots_to_drop = new ByTable(new object [] { 10, 5, 3 });
		}

		public Organ_External_RHand ( dynamic P = null ) : base( (object)(P) ) {
			
		}

		// Function from file: organ_external.dm
		public override Obj_Item generate_dropped_organ( Obj_Item current_organ = null ) {
			
			if ( this.is_peg() != 0 ) {
				current_organ = new Obj_Item_Stack_Sheet_Wood( this.owner.loc );
			}

			if ( !( current_organ != null ) ) {
				
				if ( !( this.is_robotic() != 0 ) ) {
					current_organ = new Obj_Item_Weapon_Organ_RHand( this.owner.loc, this.owner );
				}
			}
			return current_organ;
		}

		// Function from file: organ_external.dm
		public override bool process(  ) {
			base.process();
			this.process_grasp( this.owner.r_hand, "right hand" );
			return false;
		}

	}

}