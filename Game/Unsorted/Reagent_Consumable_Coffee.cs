// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Consumable_Coffee : Reagent_Consumable {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Coffee";
			this.id = "coffee";
			this.description = "Coffee is a brewed drink prepared from roasted seeds, commonly called coffee beans, of the coffee plant.";
			this.color = "#482000";
			this.nutriment_factor = 0;
			this.overdose_threshold = 80;
		}

		// Function from file: drink_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			M.dizziness = Num13.MaxInt( 0, M.dizziness - 5 );
			M.drowsyness = Num13.MaxInt( 0, M.drowsyness - 3 );
			((Mob)M).AdjustSleeping( -2 );

			if ( Convert.ToDouble( M.bodytemperature ) < 310 ) {
				M.bodytemperature = Num13.MinInt( 310, Convert.ToInt32( M.bodytemperature + 37.5 ) );
			}

			if ( Lang13.Bool( ((Reagents)this.holder).has_reagent( "frostoil" ) ) ) {
				((Reagents)this.holder).remove_reagent( "frostoil", 5 );
			}
			base.on_mob_life( (object)(M) );
			return false;
		}

		// Function from file: drink_reagents.dm
		public override void overdose_process( dynamic M = null ) {
			((Mob)M).Jitter( 5 );
			base.overdose_process( (object)(M) );
			return;
		}

	}

}