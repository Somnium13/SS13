// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Disease_Cold9 : Disease {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "The Cold";
			this.max_stages = 3;
			this.spread_text = "On contact";
			this.spread_flags = 32;
			this.cure_text = "Common Cold Anti-bodies & Spaceacillin";
			this.cures = new ByTable(new object [] { "spaceacillin" });
			this.agent = "ICE9-rhinovirus";
			this.viable_mobtypes = new ByTable(new object [] { typeof(Mob_Living_Carbon_Human) });
			this.desc = "If left untreated the subject will slow, as if partly frozen.";
			this.severity = "Medium";
		}

		// Function from file: cold9.dm
		public override void stage_act(  ) {
			base.stage_act();

			switch ((int?)( this.stage )) {
				case 2:
					this.affected_mob.bodytemperature -= 10;

					if ( Rand13.PercentChance( 1 ) && Rand13.PercentChance( 10 ) ) {
						this.affected_mob.WriteMsg( "<span class='notice'>You feel better.</span>" );
						this.cure();
						return;
					}

					if ( Rand13.PercentChance( 1 ) ) {
						((Mob)this.affected_mob).emote( "sneeze" );
					}

					if ( Rand13.PercentChance( 1 ) ) {
						((Mob)this.affected_mob).emote( "cough" );
					}

					if ( Rand13.PercentChance( 1 ) ) {
						this.affected_mob.WriteMsg( "<span class='danger'>Your throat feels sore.</span>" );
					}

					if ( Rand13.PercentChance( 5 ) ) {
						this.affected_mob.WriteMsg( "<span class='danger'>You feel stiff.</span>" );
					}
					break;
				case 3:
					this.affected_mob.bodytemperature -= 20;

					if ( Rand13.PercentChance( 1 ) ) {
						((Mob)this.affected_mob).emote( "sneeze" );
					}

					if ( Rand13.PercentChance( 1 ) ) {
						((Mob)this.affected_mob).emote( "cough" );
					}

					if ( Rand13.PercentChance( 1 ) ) {
						this.affected_mob.WriteMsg( "<span class='danger'>Your throat feels sore.</span>" );
					}

					if ( Rand13.PercentChance( 10 ) ) {
						this.affected_mob.WriteMsg( "<span class='danger'>You feel stiff.</span>" );
					}
					break;
			}
			return;
		}

	}

}