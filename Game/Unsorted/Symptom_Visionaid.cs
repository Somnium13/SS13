// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Symptom_Visionaid : Symptom {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Ocular Restoration";
			this.stealth = -1;
			this.resistance = -3;
			this.stage_speed = -2;
			this.transmittable = -4;
			this.level = 4;
		}

		// Function from file: vision.dm
		public override void Activate( Disease_Advance A = null ) {
			dynamic M = null;

			base.Activate( A );

			if ( Rand13.PercentChance( 15 ) ) {
				M = A.affected_mob;

				switch ((int?)( A.stage )) {
					case 4:
					case 5:
						
						if ( ( ((Reagents)M.reagents).get_reagent_amount( "oculine" ) ?1:0) < 20 ) {
							((Reagents)M.reagents).add_reagent( "oculine", 20 );
						}
						break;
					default:
						
						if ( Rand13.PercentChance( 15 ) ) {
							M.WriteMsg( "<span class='notice'>" + Rand13.Pick(new object [] { "Your eyes feel great.", "You are now blinking manually.", "You don't feel the need to blink." }) + "</span>" );
						}
						break;
				}
			}
			return;
		}

	}

}