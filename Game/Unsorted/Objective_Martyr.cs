// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Martyr : Objective {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.explanation_text = "Die a glorious death.";
			this.dangerrating = 1;
		}

		public Objective_Martyr ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override int check_completion(  ) {
			
			if ( !Lang13.Bool( this.owner.current ) ) {
				return 1;
			}

			if ( Lang13.Bool( this.owner.current ) && Convert.ToInt32( this.owner.current.stat ) == 2 ) {
				return 1;
			}
			return 0;
		}

	}

}