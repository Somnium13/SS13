// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Medicine_Ephedrine : Reagent_Medicine {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Ephedrine";
			this.id = "ephedrine";
			this.description = "Increases stun resistance and movement speed. Overdose deals toxin damage and inhibits breathing.";
			this.color = "#C8A5DC";
			this.metabolization_rate = 0.2;
			this.overdose_threshold = 45;
			this.addiction_threshold = 30;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage4( dynamic M = null ) {
			
			if ( Rand13.PercentChance( 33 ) ) {
				((Mob_Living)M).adjustToxLoss( 5 );
				M.losebreath += 5;
			}
			base.addiction_act_stage4( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage3( dynamic M = null ) {
			
			if ( Rand13.PercentChance( 33 ) ) {
				((Mob_Living)M).adjustToxLoss( 4 );
				M.losebreath += 4;
			}
			base.addiction_act_stage3( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage2( dynamic M = null ) {
			
			if ( Rand13.PercentChance( 33 ) ) {
				((Mob_Living)M).adjustToxLoss( 3 );
				M.losebreath += 3;
			}
			base.addiction_act_stage2( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage1( dynamic M = null ) {
			
			if ( Rand13.PercentChance( 33 ) ) {
				((Mob_Living)M).adjustToxLoss( 2 );
				M.losebreath += 2;
			}
			base.addiction_act_stage1( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void overdose_process( dynamic M = null ) {
			
			if ( Rand13.PercentChance( 33 ) ) {
				((Mob_Living)M).adjustToxLoss( 0.5 );
				M.losebreath++;
			}
			base.overdose_process( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			M.status_flags |= 32;
			((Mob)M).AdjustParalysis( -1 );
			((Mob)M).AdjustStunned( -1 );
			((Mob)M).AdjustWeakened( -1 );
			((Mob_Living)M).adjustStaminaLoss( -1 );
			base.on_mob_life( (object)(M) );
			return false;
		}

	}

}