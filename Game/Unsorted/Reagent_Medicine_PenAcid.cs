// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Medicine_PenAcid : Reagent_Medicine {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Pentetic Acid";
			this.id = "pen_acid";
			this.description = "Reduces massive amounts of radiation and toxin damage while purging other chemicals from the body. Has a chance of dealing brute damage.";
			this.color = "#C8A5DC";
			this.metabolization_rate = 0.2;
		}

		// Function from file: medicine_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			Reagent R = null;

			
			if ( M.radiation > 0 ) {
				M.radiation -= 4;
			}
			((Mob_Living)M).adjustToxLoss( -2 );

			if ( M.radiation < 0 ) {
				M.radiation = 0;
			}

			foreach (dynamic _a in Lang13.Enumerate( M.reagents.reagent_list, typeof(Reagent) )) {
				R = _a;
				

				if ( R != this ) {
					((Reagents)M.reagents).remove_reagent( R.id, 2 );
				}
			}
			base.on_mob_life( (object)(M) );
			return false;
		}

	}

}