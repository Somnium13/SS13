// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Outfit : Game_Data {

		public string name = "Naked";
		public Type uniform = null;
		public Type suit = null;
		public Type back = null;
		public Type belt = null;
		public Type gloves = null;
		public Type shoes = null;
		public Type head = null;
		public Type mask = null;
		public Type ears = null;
		public Type glasses = null;
		public Type id = null;
		public Type l_pocket = null;
		public Type r_pocket = null;
		public Type suit_store = null;
		public Type r_hand = null;
		public Type l_hand = null;
		public ByTable backpack_contents = new ByTable();

		// Function from file: outfit.dm
		public bool equip( Mob H = null, int? visualsOnly = null ) {
			visualsOnly = visualsOnly ?? GlobalVars.FALSE;

			dynamic path = null;
			int? number = null;
			int? i = null;

			this.pre_equip( H, visualsOnly );

			if ( this.uniform != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.uniform, H ), 14 );
			}

			if ( this.suit != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.suit, H ), 13 );
			}

			if ( this.back != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.back, H ), 1 );
			}

			if ( this.belt != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.belt, H ), 6 );
			}

			if ( this.gloves != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.gloves, H ), 10 );
			}

			if ( this.shoes != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.shoes, H ), 12 );
			}

			if ( this.head != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.head, H ), 11 );
			}

			if ( this.mask != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.mask, H ), 2 );
			}

			if ( this.ears != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.ears, H ), 8 );
			}

			if ( this.glasses != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.glasses, H ), 9 );
			}

			if ( this.id != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.id, H ), 7 );
			}

			if ( this.l_pocket != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.l_pocket, H ), 15 );
			}

			if ( this.r_pocket != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.r_pocket, H ), 16 );
			}

			if ( this.suit_store != null ) {
				H.equip_to_slot_or_del( Lang13.Call( this.suit_store, H ), 17 );
			}

			if ( this.l_hand != null ) {
				H.put_in_l_hand( Lang13.Call( this.l_hand, H ) );
			}

			if ( this.r_hand != null ) {
				H.put_in_r_hand( Lang13.Call( this.r_hand, H ) );
			}

			foreach (dynamic _a in Lang13.Enumerate( this.backpack_contents )) {
				path = _a;
				
				number = Lang13.IntNullable( this.backpack_contents[path] );
				i = null;
				i = 0;

				while (( i ??0) < ( number ??0)) {
					H.equip_to_slot_or_del( Lang13.Call( path, H ), 18 );
					i++;
				}
			}
			this.post_equip( H, visualsOnly );
			return true;
		}

		// Function from file: outfit.dm
		public virtual void post_equip( Mob H = null, int? visualsOnly = null ) {
			visualsOnly = visualsOnly ?? GlobalVars.FALSE;

			return;
		}

		// Function from file: outfit.dm
		public virtual void pre_equip( Mob H = null, int? visualsOnly = null ) {
			visualsOnly = visualsOnly ?? GlobalVars.FALSE;

			return;
		}

	}

}