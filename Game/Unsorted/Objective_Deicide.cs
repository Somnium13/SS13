// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Deicide : Objective {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.dangerrating = 20;
			this.martyr_compatible = true;
		}

		public Objective_Deicide ( string text = null ) : base( text ) {
			
		}

		// Function from file: objectives.dm
		public override void update_explanation_text(  ) {
			base.update_explanation_text();

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = "Phase " + this.target.name + ", the false god, out of this plane of existence..";
			} else {
				this.explanation_text = "Free Objective";
			}
			return;
		}

		// Function from file: objectives.dm
		public override dynamic find_target(  ) {
			dynamic G = null;

			
			if ( !Lang13.Bool( this.owner ) || !Lang13.Bool( this.owner.current ) ) {
				return null;
			}

			if ( GlobalFuncs.is_handofgod_god( this.owner.current ) ) {
				G = this.owner.current;

				if ( G.side == "red" ) {
					this.target = GlobalVars.ticker.mode.blue_deities[1];
				}

				if ( G.side == "blue" ) {
					this.target = GlobalVars.ticker.mode.red_deities[1];
				}
			}
			this.update_explanation_text();
			return null;
		}

		// Function from file: objectives.dm
		public override int check_completion(  ) {
			
			if ( Lang13.Bool( this.target ) ) {
				
				if ( Lang13.Bool( this.target.current ) ) {
					return 0;
				}
			}
			return 1;
		}

	}

}