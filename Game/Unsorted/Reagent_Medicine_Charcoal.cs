// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Medicine_Charcoal : Reagent_Medicine {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Charcoal";
			this.id = "charcoal";
			this.description = "Heals toxin damage as well as slowly removing any other chemicals the patient has in their bloodstream.";
			this.color = "#C8A5DC";
			this.metabolization_rate = 0.2;
		}

		// Function from file: medicine_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			Reagent R = null;

			((Mob_Living)M).adjustToxLoss( -2 );

			foreach (dynamic _a in Lang13.Enumerate( M.reagents.reagent_list, typeof(Reagent) )) {
				R = _a;
				

				if ( R != this ) {
					((Reagents)M.reagents).remove_reagent( R.id, 1 );
				}
			}
			base.on_mob_life( (object)(M) );
			return false;
		}

	}

}