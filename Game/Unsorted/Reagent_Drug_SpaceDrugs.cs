// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Drug_SpaceDrugs : Reagent_Drug {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Space drugs";
			this.id = "space_drugs";
			this.description = "An illegal chemical compound used as drug.";
			this.color = "#60A584";
			this.overdose_threshold = 30;
		}

		// Function from file: drug_reagents.dm
		public override void overdose_process( dynamic M = null ) {
			
			if ( M.hallucination < this.volume && Rand13.PercentChance( 20 ) ) {
				M.hallucination += 5;
			}
			base.overdose_process( (object)(M) );
			return;
		}

		// Function from file: drug_reagents.dm
		public override void overdose_start( dynamic M = null ) {
			M.WriteMsg( "<span class='userdanger'>You start tripping hard!</span>" );
			return;
		}

		// Function from file: drug_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			((Mob)M).set_drugginess( 15 );

			if ( M.loc is Tile && !( M.loc is Tile_Space ) ) {
				
				if ( Lang13.Bool( M.canmove ) ) {
					
					if ( Rand13.PercentChance( 10 ) ) {
						Map13.Step( M, Convert.ToInt32( Rand13.PickFromTable( GlobalVars.cardinal ) ) );
					}
				}
			}

			if ( Rand13.PercentChance( 7 ) ) {
				((Mob)M).emote( Rand13.Pick(new object [] { "twitch", "drool", "moan", "giggle" }) );
			}
			base.on_mob_life( (object)(M) );
			return false;
		}

	}

}