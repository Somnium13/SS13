// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Hijack : Objective {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.explanation_text = "Hijack the shuttle to ensure no loyalist Nanotrasen crew escape alive and out of custody.";
			this.dangerrating = 25;
		}

		public Objective_Hijack ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override int check_completion(  ) {
			dynamic A = null;
			Mob_Living player = null;

			
			if ( !Lang13.Bool( this.owner.current ) || Lang13.Bool( this.owner.current.stat ) ) {
				return 0;
			}

			if ( GlobalVars.SSshuttle.emergency.mode < 6 ) {
				return 0;
			}

			if ( this.owner.current is Mob_Living_Silicon ) {
				return 0;
			}
			A = GlobalFuncs.get_area( this.owner.current );

			if ( GlobalVars.SSshuttle.emergency.areaInstance != A ) {
				return 0;
			}

			foreach (dynamic _a in Lang13.Enumerate( GlobalVars.player_list, typeof(Mob_Living) )) {
				player = _a;
				

				if ( player.mind != null && player.mind != this.owner ) {
					
					if ( player.stat != 2 ) {
						
						if ( player is Mob_Living_Silicon ) {
							continue;
						}

						if ( GlobalFuncs.get_area( player ) == A ) {
							
							if ( !Lang13.Bool( player.mind.special_role ) && !( GlobalFuncs.get_turf( player.mind.current ) is Tile_Simulated_Floor_Plasteel_Shuttle_Red ) ) {
								return 0;
							}
						}
					}
				}
			}
			return 1;
		}

	}

}