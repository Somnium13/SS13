// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Symptom_Visionloss : Symptom {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Hyphema";
			this.stealth = -1;
			this.resistance = -4;
			this.stage_speed = -4;
			this.transmittable = -3;
			this.level = 5;
			this.severity = 4;
		}

		// Function from file: vision.dm
		public override void Activate( Disease_Advance A = null ) {
			dynamic M = null;

			base.Activate( A );

			if ( Rand13.PercentChance( GlobalVars.SYMPTOM_ACTIVATION_PROB ) ) {
				M = A.affected_mob;

				switch ((int?)( A.stage )) {
					case 1:
					case 2:
						M.WriteMsg( "<span class='warning'>Your eyes itch.</span>" );
						break;
					case 3:
					case 4:
						M.WriteMsg( "<span class='warning'><b>Your eyes burn!</b></span>" );
						((Mob)M).blur_eyes( 10 );
						((Mob)M).adjust_eye_damage( 1 );
						break;
					default:
						M.WriteMsg( "<span class='userdanger'>Your eyes burn horrificly!</span>" );
						((Mob)M).blur_eyes( 20 );
						((Mob)M).adjust_eye_damage( 5 );

						if ( M.eye_damage >= 10 ) {
							((Mob)M).become_nearsighted();

							if ( Rand13.PercentChance( M.eye_damage - 10 + 1 ) ) {
								
								if ( ((Mob)M).become_blind() ) {
									M.WriteMsg( "<span class='userdanger'>You go blind!</span>" );
								}
							}
						}
						break;
				}
			}
			return;
		}

	}

}