// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_SummonGuns : Objective {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.explanation_text = "Steal at least five guns!";
		}

		public Objective_SummonGuns ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override int check_completion(  ) {
			int guncount = 0;
			ByTable all_items = null;
			Obj I = null;

			
			if ( !( this.owner.current is Mob_Living ) ) {
				return 0;
			}
			guncount = 0;
			all_items = ((Ent_Static)this.owner.current).GetAllContents();

			foreach (dynamic _a in Lang13.Enumerate( all_items, typeof(Obj) )) {
				I = _a;
				

				if ( I is Obj_Item_Weapon_Gun ) {
					guncount++;
				}
			}

			if ( guncount >= 5 ) {
				return 1;
			} else {
				return 0;
			}
		}

	}

}