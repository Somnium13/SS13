// FILE AUTOGENERATED BY SOMNIUM13.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Wires_Radio : Wires {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.holder_type = typeof(Obj_Item_Device_Radio);
		}

		// Function from file: radio.dm
		public Wires_Radio ( Obj_Item_Device_Radio holder = null ) : base( holder ) {
			this.wires = new ByTable(new object [] { "signal", "recieve", "transmit" });
			// Warning: Super call was HERE! If anything above HERE is needed by the super call, it might break!;
			return;
		}

		// Function from file: radio.dm
		public override void on_pulse( string wire = null ) {
			Obj R = null;

			R = this.holder;

			switch ((string)( wire )) {
				case "signal":
					((dynamic)R).listening = !Lang13.Bool( ((dynamic)R).listening );
					((dynamic)R).broadcasting = ((dynamic)R).listening;
					break;
				case "recieve":
					((dynamic)R).listening = !Lang13.Bool( ((dynamic)R).listening );
					break;
				case "transmit":
					((dynamic)R).broadcasting = !Lang13.Bool( ((dynamic)R).broadcasting );
					break;
			}
			return;
		}

		// Function from file: radio.dm
		public override int? interactable( dynamic user = null ) {
			Obj R = null;

			R = this.holder;

			if ( Lang13.Bool( ((dynamic)R).b_stat ) ) {
				return GlobalVars.TRUE;
			}
			return null;
		}

	}

}