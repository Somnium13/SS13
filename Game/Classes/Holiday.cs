// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Holiday : Game_Data {

		public string name = "Bugsgiving";
		public double? begin_day = 1;
		public double? begin_month = 0;
		public double? end_day = 0;
		public double? end_month = 0;

		// Function from file: holidays.dm
		public virtual bool shouldCelebrate( double? dd = null, double? mm = null, double? yy = null ) {
			
			if ( !Lang13.Bool( this.end_day ) ) {
				this.end_day = this.begin_day;
			}

			if ( !Lang13.Bool( this.end_month ) ) {
				this.end_month = this.begin_month;
			}

			if ( ( this.end_month ??0) > ( this.begin_month ??0) ) {
				
				if ( mm == this.end_month ) {
					
					if ( ( dd ??0) <= ( this.end_day ??0) ) {
						return true;
					}
				} else if ( mm == this.begin_month ) {
					
					if ( ( dd ??0) >= ( this.begin_day ??0) ) {
						return true;
					}
				} else if ( Lang13.IsInRange( mm ??0, this.begin_month ??0, this.end_month ??0 ) ) {
					return true;
				}
			} else if ( this.end_month == this.begin_month ) {
				
				if ( mm == this.begin_month && Lang13.IsInRange( dd ??0, this.begin_day ??0, this.end_day ??0 ) ) {
					return true;
				}
			} else {
				
				if ( ( mm ??0) >= ( this.begin_month ??0) && ( dd ??0) >= ( this.begin_day ??0) ) {
					return true;
				}

				if ( ( mm ??0) <= ( this.end_month ??0) && ( dd ??0) <= ( this.end_day ??0) ) {
					return true;
				}
			}
			return false;
		}

		// Function from file: holidays.dm
		public virtual string getStationPrefix(  ) {
			int i = 0;

			i = String13.FindIgnoreCase( this.name, " ", 1, 0 );
			return String13.SubStr( this.name, 1, i );
		}

		// Function from file: holidays.dm
		public virtual string greet(  ) {
			return "Have a happy " + this.name + "!";
		}

		// Function from file: holidays.dm
		public void celebrate(  ) {
			return;
		}

	}

}