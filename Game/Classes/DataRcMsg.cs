// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class DataRcMsg : Game_Data {

		public string rec_dpt = "Unspecified";
		public string send_dpt = "Unspecified";
		public string message = "Blank";
		public string stamp = "Unstamped";
		public string id_auth = "Unauthenticated";
		public string priority = "Normal";

		// Function from file: message_server.dm
		public DataRcMsg ( string param_rec = null, string param_sender = null, string param_message = null, string param_stamp = null, string param_id_auth = null, dynamic param_priority = null ) {
			param_rec = param_rec ?? "";
			param_sender = param_sender ?? "";
			param_message = param_message ?? "";
			param_stamp = param_stamp ?? "";
			param_id_auth = param_id_auth ?? "";

			
			if ( Lang13.Bool( param_rec ) ) {
				this.rec_dpt = param_rec;
			}

			if ( Lang13.Bool( param_sender ) ) {
				this.send_dpt = param_sender;
			}

			if ( Lang13.Bool( param_message ) ) {
				this.message = param_message;
			}

			if ( Lang13.Bool( param_stamp ) ) {
				this.stamp = param_stamp;
			}

			if ( Lang13.Bool( param_id_auth ) ) {
				this.id_auth = param_id_auth;
			}

			if ( Lang13.Bool( param_priority ) ) {
				
				dynamic _a = param_priority; // Was a switch-case, sorry for the mess.
				if ( _a==1 ) {
					this.priority = "Normal";
				} else if ( _a==2 ) {
					this.priority = "High";
				} else if ( _a==3 ) {
					this.priority = "Extreme";
				} else {
					this.priority = "Undetermined";
				}
			}
			return;
		}

	}

}