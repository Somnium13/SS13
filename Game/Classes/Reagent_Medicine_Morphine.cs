// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Medicine_Morphine : Reagent_Medicine {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Morphine";
			this.id = "morphine";
			this.description = "A painkiller that allows the patient to move at full speed even in bulky objects. Causes drowsiness and eventually unconsciousness in high doses. Overdose will cause a variety of effects, ranging from minor to lethal.";
			this.color = "#C8A5DC";
			this.metabolization_rate = 0.2;
			this.overdose_threshold = 30;
			this.addiction_threshold = 25;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage4( dynamic M = null ) {
			dynamic I = null;

			
			if ( Rand13.PercentChance( 33 ) ) {
				I = ((Mob)M).get_active_hand();

				if ( Lang13.Bool( I ) ) {
					M.drop_item();
				}
				((Mob_Living)M).adjustToxLoss( 3 );
				((Mob)M).Dizzy( 5 );
				((Mob)M).Jitter( 5 );
			}
			base.addiction_act_stage4( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage3( dynamic M = null ) {
			dynamic I = null;

			
			if ( Rand13.PercentChance( 33 ) ) {
				I = ((Mob)M).get_active_hand();

				if ( Lang13.Bool( I ) ) {
					M.drop_item();
				}
				((Mob_Living)M).adjustToxLoss( 2 );
				((Mob)M).Dizzy( 4 );
				((Mob)M).Jitter( 4 );
			}
			base.addiction_act_stage3( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage2( dynamic M = null ) {
			dynamic I = null;

			
			if ( Rand13.PercentChance( 33 ) ) {
				I = ((Mob)M).get_active_hand();

				if ( Lang13.Bool( I ) ) {
					M.drop_item();
				}
				((Mob_Living)M).adjustToxLoss( 1 );
				((Mob)M).Dizzy( 3 );
				((Mob)M).Jitter( 3 );
			}
			base.addiction_act_stage2( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void addiction_act_stage1( dynamic M = null ) {
			dynamic I = null;

			
			if ( Rand13.PercentChance( 33 ) ) {
				I = ((Mob)M).get_active_hand();

				if ( Lang13.Bool( I ) ) {
					M.drop_item();
				}
				((Mob)M).Dizzy( 2 );
				((Mob)M).Jitter( 2 );
			}
			base.addiction_act_stage1( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override void overdose_process( dynamic M = null ) {
			dynamic I = null;

			
			if ( Rand13.PercentChance( 33 ) ) {
				I = ((Mob)M).get_active_hand();

				if ( Lang13.Bool( I ) ) {
					((Mob)M).drop_item();
				}
				((Mob)M).Dizzy( 2 );
				((Mob)M).Jitter( 2 );
			}
			base.overdose_process( (object)(M) );
			return;
		}

		// Function from file: medicine_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			M.status_flags |= 16;

			if ( this.current_cycle == 11 ) {
				M.WriteMsg( "<span class='warning'>You start to feel tired...</span>" );
			}

			if ( this.current_cycle >= 12 && this.current_cycle < 24 ) {
				M.drowsyness += 1;
			} else if ( this.current_cycle >= 24 ) {
				M.sleeping += 1;
			}
			base.on_mob_life( (object)(M) );
			return false;
		}

	}

}