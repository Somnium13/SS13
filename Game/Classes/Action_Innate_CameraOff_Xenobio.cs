// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Action_Innate_CameraOff_Xenobio : Action_Innate_CameraOff {

		public Action_Innate_CameraOff_Xenobio ( dynamic Target = null ) : base( (object)(Target) ) {
			
		}

		// Function from file: xenobio_camera.dm
		public override void Activate(  ) {
			dynamic C = null;
			Obj remote_eye = null;
			Obj_Machinery_Computer_CameraAdvanced origin = null;
			Camerachunk chunk = null;

			
			if ( !Lang13.Bool( this.target ) || !( this.target is Mob_Living_Carbon_Human ) ) {
				return;
			}
			C = this.target;
			remote_eye = C.remote_control;
			origin = ((dynamic)remote_eye).origin;
			C.remote_view = false;
			origin.current_user = null;
			origin.jump_action.Remove( C );
			((dynamic)origin).slime_place_action.Remove( C );
			((dynamic)origin).slime_up_action.Remove( C );
			((dynamic)origin).feed_slime_action.Remove( C );
			((dynamic)origin).monkey_recycle_action.Remove( C );
			((dynamic)remote_eye).user = null;

			if ( Lang13.Bool( C.client ) ) {
				C.client.perspective = GlobalVars.MOB_PERSPECTIVE ?1:0;
				C.client.eye = this;
				C.client.images.Remove( ((dynamic)remote_eye).user_image );

				foreach (dynamic _a in Lang13.Enumerate( ((dynamic)remote_eye).visibleCameraChunks, typeof(Camerachunk) )) {
					chunk = _a;
					
					C.client.images.Remove( chunk.obscured );
				}
			}
			C.remote_control = null;
			((Mob)C).unset_machine();
			this.Remove( C );
			return;
		}

	}

}