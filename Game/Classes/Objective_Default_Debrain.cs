// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Objective_Default_Debrain : Objective_Default {

		public bool? target_role_type = false;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.dangerrating = 20;
			this.randomgen = false;
		}

		public Objective_Default_Debrain ( string text = null ) : base( text ) {
			
		}

		// Function from file: objective.dm
		public override void update_explanation_text(  ) {
			base.update_explanation_text();

			if ( Lang13.Bool( this.target ) && Lang13.Bool( this.target.current ) ) {
				this.explanation_text = "Steal the brain of " + this.target.name + ", the " + ( !( this.target_role_type == true ) ? this.target.assigned_role : ((dynamic)( this.target.special_role )) ) + ".";
			} else {
				this.explanation_text = "Free Objective";
			}
			return;
		}

		// Function from file: objective.dm
		public override int check_completion(  ) {
			Ent_Static A = null;

			
			if ( !Lang13.Bool( this.target ) ) {
				return 1;
			}

			if ( !Lang13.Bool( this.owner.current ) || Convert.ToInt32( this.owner.current.stat ) == 2 ) {
				return 0;
			}

			if ( !Lang13.Bool( this.target.current ) || !( this.target.current is Mob_Living_Carbon_Brain ) ) {
				return 0;
			}
			A = this.target.current;

			while (A.loc != null) {
				A = A.loc;

				if ( A == this.owner.current ) {
					return 1;
				}
			}
			return 0;
		}

		// Function from file: objective.dm
		public override dynamic find_target_by_role( string role = null, bool? role_type = null, bool? invert = null ) {
			role_type = role_type ?? false;
			invert = invert ?? false;

			
			if ( !( invert == true ) ) {
				this.target_role_type = role_type;
			}
			base.find_target_by_role( role, role_type, invert );
			return this.target;
		}

	}

}