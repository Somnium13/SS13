// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Machinery_KeycardAuth : Obj_Machinery {

		public bool active = false;
		public string v_event = "";
		public int screen = 1;
		public bool confirmed = false;
		public int confirm_delay = 20;
		public bool busy = false;
		public Obj_Machinery_KeycardAuth event_source = null;
		public Mob event_triggered_by = null;
		public Mob event_confirmed_by = null;

		protected override void __FieldInit() {
			base.__FieldInit();

			this.anchored = 1;
			this.idle_power_usage = 2;
			this.active_power_usage = 6;
			this.power_channel = 3;
			this.icon = "icons/obj/monitors.dmi";
			this.icon_state = "auth_off";
		}

		public Obj_Machinery_KeycardAuth ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: keycard authentication.dm
		public void trigger_event(  ) {
			
			switch ((string)( this.v_event )) {
				case "Red alert":
					GlobalFuncs.set_security_level( 2 );
					GlobalFuncs.feedback_inc( "alert_keycard_auth_red", 1 );
					break;
				case "Emergency Maintenance Access":
					GlobalFuncs.make_maint_all_access();
					GlobalFuncs.feedback_inc( "alert_keycard_auth_maint", 1 );
					break;
			}
			return;
		}

		// Function from file: keycard authentication.dm
		public void receive_request( Obj_Machinery_KeycardAuth source = null ) {
			
			if ( ( this.stat & 3 ) != 0 ) {
				return;
			}
			this.event_source = source;
			this.busy = true;
			this.active = true;
			this.icon_state = "auth_on";
			Task13.Sleep( this.confirm_delay );
			this.event_source = null;
			this.icon_state = "auth_off";
			this.active = false;
			this.busy = false;
			return;
		}

		// Function from file: keycard authentication.dm
		public void broadcast_request(  ) {
			Obj_Machinery_KeycardAuth KA = null;

			this.icon_state = "auth_on";

			foreach (dynamic _a in Lang13.Enumerate( GlobalVars.machines, typeof(Obj_Machinery_KeycardAuth) )) {
				KA = _a;
				

				if ( KA == this ) {
					continue;
				}
				KA.reset();
				Task13.Schedule( 0, (Task13.Closure)(() => {
					KA.receive_request( this );
					return;
				}));
			}
			Task13.Sleep( this.confirm_delay );

			if ( this.confirmed ) {
				this.confirmed = false;
				this.trigger_event(  );
				GlobalFuncs.log_game( "" + GlobalFuncs.key_name( this.event_triggered_by ) + " triggered and " + GlobalFuncs.key_name( this.event_confirmed_by ) + " confirmed event " + this.v_event );
				GlobalFuncs.message_admins( "" + GlobalFuncs.key_name( this.event_triggered_by ) + " triggered and " + GlobalFuncs.key_name( this.event_confirmed_by ) + " confirmed event " + this.v_event );
			}
			this.reset();
			return;
		}

		// Function from file: keycard authentication.dm
		public void reset(  ) {
			this.active = false;
			this.v_event = "";
			this.screen = 1;
			this.confirmed = false;
			this.event_source = null;
			this.icon_state = "auth_off";
			this.event_triggered_by = null;
			this.event_confirmed_by = null;
			return;
		}

		// Function from file: keycard authentication.dm
		public override dynamic Topic( string href = null, ByTable href_list = null, dynamic hsrc = null ) {
			
			if ( Lang13.Bool( base.Topic( href, href_list, (object)(hsrc) ) ) ) {
				return null;
			}

			if ( this.busy ) {
				Task13.User.WriteMsg( "This device is busy." );
				return null;
			}

			if ( Lang13.Bool( href_list["triggerevent"] ) ) {
				this.v_event = href_list["triggerevent"];
				this.screen = 2;
			}

			if ( Lang13.Bool( href_list["reset"] ) ) {
				this.reset();
			}
			this.updateUsrDialog();
			return null;
		}

		// Function from file: keycard authentication.dm
		public override dynamic attack_hand( dynamic a = null, bool? b = null, bool? c = null ) {
			string dat = null;

			
			if ( Lang13.Bool( a.stat ) || ( this.stat & 3 ) != 0 ) {
				a.WriteMsg( "<span class='warning'>This device is not powered!</span>" );
				return null;
			}

			if ( this.busy ) {
				a.WriteMsg( "<span class='warning'>This device is busy!</span>" );
				return null;
			}
			((Mob)a).set_machine( this );
			dat = "<h1>Keycard Authentication Device</h1>";
			dat += "This device is used to trigger some high security events. It requires the simultaneous swipe of two high-level ID cards.";
			dat += "<br><hr><br>";

			if ( this.screen == 1 ) {
				dat += "Select an event to trigger:<ul>";
				dat += new Txt( "<li><A href='?src=" ).Ref( this ).str( ";triggerevent=Red alert'>Red alert</A></li>" ).ToString();
				dat += new Txt( "<li><A href='?src=" ).Ref( this ).str( ";triggerevent=Emergency Maintenance Access'>Emergency Maintenance Access</A></li>" ).ToString();
				dat += "</ul>";
				Interface13.Browse( a, dat, "window=keycard_auth;size=500x250" );
			}

			if ( this.screen == 2 ) {
				dat += "Please swipe your card to authorize the following event: <b>" + this.v_event + "</b>";
				dat += new Txt( "<p><A href='?src=" ).Ref( this ).str( ";reset=1'>Back</A>" ).ToString();
				Interface13.Browse( a, dat, "window=keycard_auth;size=500x250" );
			}
			return null;
		}

		// Function from file: keycard authentication.dm
		public override void power_change(  ) {
			
			if ( Lang13.Bool( this.powered( 3 ) ) ) {
				this.stat &= 65533;
				this.icon_state = "auth_off";
			} else {
				this.stat |= 2;
			}
			return;
		}

		// Function from file: keycard authentication.dm
		public override dynamic attackby( dynamic A = null, dynamic user = null, string _params = null, bool? silent = null, bool? replace_spent = null ) {
			dynamic ID = null;

			
			if ( ( this.stat & 3 ) != 0 ) {
				user.WriteMsg( "This device is not powered." );
				return null;
			}

			if ( A is Obj_Item_Weapon_Card_Id ) {
				ID = A;

				if ( Lang13.Bool( ID.access.Contains( GlobalVars.access_keycard_auth ) ) ) {
					
					if ( this.active ) {
						
						if ( this.event_source != null ) {
							this.event_source.confirmed = true;
							this.event_source.event_confirmed_by = Task13.User;
						}
					} else if ( this.screen == 2 ) {
						this.event_triggered_by = Task13.User;
						this.broadcast_request();
					}
				}
			}
			return null;
		}

		// Function from file: keycard authentication.dm
		public override dynamic attack_paw( dynamic a = null, dynamic b = null, dynamic c = null ) {
			a.WriteMsg( "<span class='warning'>You are too primitive to use this device!</span>" );
			return null;
		}

		// Function from file: keycard authentication.dm
		public override dynamic attack_ai( dynamic user = null ) {
			
			if ( GlobalFuncs.IsAdminGhost( user ) ) {
				return null;
			}
			user.WriteMsg( "The station AI is not to interact with these devices." );
			return null;
		}

	}

}