// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Effect_Spider_Cocoon : Obj_Effect_Spider {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.health = 60;
			this.icon_state = "cocoon1";
		}

		// Function from file: spiders.dm
		public Obj_Effect_Spider_Cocoon ( dynamic loc = null ) : base( (object)(loc) ) {
			this.icon_state = Rand13.Pick(new object [] { "cocoon1", "cocoon2", "cocoon3" });
			return;
		}

		// Function from file: spiders.dm
		public override dynamic Destroy(  ) {
			Ent_Dynamic A = null;

			this.visible_message( new Txt( "<span class='warning'>" ).The( this ).item().str( " splits open.</span>" ).ToString() );

			foreach (dynamic _a in Lang13.Enumerate( this.contents, typeof(Ent_Dynamic) )) {
				A = _a;
				
				A.loc = this.loc;
			}
			return base.Destroy();
		}

		// Function from file: spiders.dm
		public override void container_resist(  ) {
			Mob user = null;
			int breakout_time = 0;

			user = Task13.User;
			breakout_time = 2;
			user.changeNext_move( 100 );
			((dynamic)user).last_special = Game13.time + 100;
			user.WriteMsg( "<span class='notice'>You struggle against the tight bonds... (This will take about " + breakout_time + " minutes.)</span>" );
			this.visible_message( new Txt( "You see something struggling and writhing in " ).the( this ).item().str( "!" ).ToString() );

			if ( GlobalFuncs.do_after( user, breakout_time * 600, null, this ) ) {
				
				if ( !( user != null ) || user.stat != 0 || user.loc != this ) {
					return;
				}
				GlobalFuncs.qdel( this );
			}
			return;
		}

	}

}