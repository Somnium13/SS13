// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Toxin_Histamine : Reagent_Toxin {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Histamine";
			this.id = "histamine";
			this.description = "Histamine's effects become more dangerous depending on the dosage amount. They range from mildly annoying to incredibly lethal.";
			this.metabolization_rate = 0.1;
			this.overdose_threshold = 30;
			this.toxpwr = 0;
		}

		// Function from file: toxin_reagents.dm
		public override void overdose_process( dynamic M = null ) {
			((Mob_Living)M).adjustOxyLoss( 2 );
			((Mob_Living)M).adjustBruteLoss( 2 );
			((Mob_Living)M).adjustToxLoss( 2 );
			base.overdose_process( (object)(M) );
			return;
		}

		// Function from file: toxin_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			
			if ( Rand13.PercentChance( 50 ) ) {
				
				dynamic _a = Rand13.Pick(new object [] { 1, 2, 3, 4 }); // Was a switch-case, sorry for the mess.
				if ( _a==1 ) {
					M.WriteMsg( "<span class='danger'>You can barely see!</span>" );
					M.eye_blurry = 3;
				} else if ( _a==2 ) {
					((Mob)M).emote( "cough" );
				} else if ( _a==3 ) {
					((Mob)M).emote( "sneeze" );
				} else if ( _a==4 ) {
					
					if ( Rand13.PercentChance( 75 ) ) {
						M.WriteMsg( "You scratch at an itch." );
						((Mob_Living)M).adjustBruteLoss( 2 );
					}
				}
			}
			base.on_mob_life( (object)(M) );
			return false;
		}

	}

}