// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Obj_Item_Weapon_Pen : Obj_Item_Weapon {

		public string colour = "black";

		protected override void __FieldInit() {
			base.__FieldInit();

			this.item_state = "pen";
			this.slot_flags = 528;
			this.w_class = 1;
			this.throw_speed = 3;
			this.materials = new ByTable().Set( "$metal", 10 );
			this.pressure_resistance = 2;
			this.icon = "icons/obj/bureaucracy.dmi";
			this.icon_state = "pen";
		}

		public Obj_Item_Weapon_Pen ( dynamic loc = null ) : base( (object)(loc) ) {
			
		}

		// Function from file: pen.dm
		public override bool attack( dynamic M = null, dynamic user = null, bool? def_zone = null ) {
			bool _default = false;

			
			if ( !( M is Mob_Living ) ) {
				return _default;
			}

			if ( !Lang13.Bool( this.force ) ) {
				
				if ( ((Mob_Living)M).can_inject( user, true ) ) {
					user.WriteMsg( "<span class='warning'>You stab " + M + " with the pen.</span>" );

					if ( !( def_zone == true ) ) {
						M.WriteMsg( "<span class='danger'>You feel a tiny prick!</span>" );
					}
					_default = true;
				}
				GlobalFuncs.add_logs( user, M, "stabbed", this );
			} else {
				_default = base.attack( (object)(M), (object)(user), def_zone );
			}
			return _default;
		}

		// Function from file: pen.dm
		public override int suicide_act( Mob_Living_Carbon_Human user = null ) {
			user.visible_message( "<span class='suicide'>" + user + " is scribbling numbers all over themself with " + this + "! It looks like they're trying to commit sudoku!</span>" );
			return 1;
		}

	}

}