// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class AtomHud_Antag : AtomHud {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.hud_icons = new ByTable(new object [] { "12" });
		}

		// Function from file: antag_hud.dm
		public void leave_hud( dynamic M = null ) {
			
			if ( !( M is Mob ) ) {
				Task13.Crash( "leave_hud(): " + M + " (" + M.type + ") is not a mob!" );
			}
			this.remove_from_hud( M );
			this.remove_hud_from( M );

			if ( Lang13.Bool( M.mind ) ) {
				M.mind.antag_hud = null;
			}
			return;
		}

		// Function from file: antag_hud.dm
		public void join_hud( dynamic M = null ) {
			
			if ( !( M is Mob ) ) {
				Task13.Crash( "join_hud(): " + M + " (" + M.type + ") is not a mob!" );
			}

			if ( M.mind.antag_hud != null ) {
				M.mind.antag_hud.leave_hud( M );
			}
			this.add_to_hud( M );
			this.add_hud_to( M );
			M.mind.antag_hud = this;
			return;
		}

	}

}