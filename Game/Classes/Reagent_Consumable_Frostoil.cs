// FILE AUTOGENERATED BY SOM13. DO NOT EDIT YET.

using System;
using Somnium.Engine.ByImpl;

namespace Somnium.Game {
	class Reagent_Consumable_Frostoil : Reagent_Consumable {

		protected override void __FieldInit() {
			base.__FieldInit();

			this.name = "Frost Oil";
			this.id = "frostoil";
			this.description = "A special oil that noticably chills the body. Extraced from Icepeppers.";
			this.color = "#8BA6E9";
		}

		// Function from file: food_reagents.dm
		public override void reaction_turf( dynamic T = null, double? volume = null ) {
			Mob_Living_SimpleAnimal_Slime M = null;

			
			if ( ( volume ??0) >= 5 ) {
				
				foreach (dynamic _a in Lang13.Enumerate( T, typeof(Mob_Living_SimpleAnimal_Slime) )) {
					M = _a;
					
					M.adjustToxLoss( Rand13.Int( 15, 30 ) );
				}
			}
			return;
		}

		// Function from file: food_reagents.dm
		public override bool on_mob_life( dynamic M = null ) {
			
			dynamic _a = this.current_cycle; // Was a switch-case, sorry for the mess.
			if ( 1<=_a&&_a<=15 ) {
				M.bodytemperature -= 15;

				if ( Lang13.Bool( ((Reagents)this.holder).has_reagent( "capsaicin" ) ) ) {
					((Reagents)this.holder).remove_reagent( "capsaicin", 5 );
				}

				if ( M is Mob_Living_SimpleAnimal_Slime ) {
					M.bodytemperature -= Rand13.Int( 5, 20 );
				}
			} else if ( 15<=_a&&_a<=25 ) {
				M.bodytemperature -= 30;

				if ( M is Mob_Living_SimpleAnimal_Slime ) {
					M.bodytemperature -= Rand13.Int( 10, 20 );
				}
			} else if ( 25<=_a&&_a<=35 ) {
				M.bodytemperature -= 45;

				if ( Rand13.PercentChance( 1 ) ) {
					((Mob)M).emote( "shiver" );
				}

				if ( M is Mob_Living_SimpleAnimal_Slime ) {
					M.bodytemperature -= Rand13.Int( 15, 20 );
				}
			} else if ( 35<=_a&&_a<=Double.PositiveInfinity ) {
				M.bodytemperature -= 60;

				if ( Rand13.PercentChance( 5 ) ) {
					((Mob)M).emote( "shiver" );
				}

				if ( M is Mob_Living_SimpleAnimal_Slime ) {
					M.bodytemperature -= Rand13.Int( 20, 25 );
				}
			}
			base.on_mob_life( (object)(M) );
			return false;
		}

	}

}